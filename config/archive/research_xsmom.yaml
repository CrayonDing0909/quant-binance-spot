# ═══════════════════════════════════════════════════════════════
# RESEARCH — XSMOM（Cross-Sectional Momentum）正式回測
# ═══════════════════════════════════════════════════════════════
# Created: 2026-02-27
# Origin: config/futures_xsmom.yaml (prototype)
# Purpose: 正式回測 XSMOM 策略，對標 production 8-symbol universe
#
# STRATEGY:
#   殘差動量排名（去除 BTC 市場因子後，跨幣種排名）
#   - 多 lookback 集成（14d/30d/60d）
#   - 跳過最近 24h（避免短期反轉效應）
#   - 波動率目標縮放
#   做多 top 30%，做空 bottom 30%
#
# HYPOTHESIS:
#   截面動量（相對強弱）與時序動量（TSMOM）相關性 ~0.3
#   → 與 production 組合低相關，有分散化價值
#
# COMPARISON TARGET:
#   prod_candidate_htf_lsr.yaml (SR=2.87, MDD=-4.75%)
# ═══════════════════════════════════════════════════════════════

market:
  symbols:
    - BTCUSDT
    - ETHUSDT
    - SOLUSDT
    - BNBUSDT
    - DOGEUSDT
    - ADAUSDT
    - AVAXUSDT
    - LINKUSDT
  interval: "1h"
  start: "2022-01-01"
  end: null
  market_type: "futures"

futures:
  leverage: 3
  margin_type: "ISOLATED"
  position_mode: "ONE_WAY"
  direction: "both"

strategy:
  name: "xsmom"
  params:
    # ═══ Universe（用於排名計算，比 trading symbols 更大以增加排名粒度）═══
    universe:
      - BTCUSDT
      - ETHUSDT
      - SOLUSDT
      - BNBUSDT
      - DOGEUSDT
      - ADAUSDT
      - AVAXUSDT
      - LINKUSDT
      - XRPUSDT
      - DOTUSDT
      - LTCUSDT
      - UNIUSDT
      - NEARUSDT
      - ATOMUSDT
      - AAVEUSDT
    data_dir: "data"
    market_symbol: "BTCUSDT"       # 市場因子（殘差計算用）

    # ═══ Momentum Signal ═══
    lookbacks: [336, 720, 1440]    # 14d / 30d / 60d
    skip_recent: 24                # 跳過最近 24h（反轉效應）
    use_residual: true             # 去除 BTC 因子

    # ═══ Ranking Thresholds ═══
    long_threshold: 0.7            # top 30% → 做多
    short_threshold: 0.3           # bottom 30% → 做空
    scale_mode: "threshold"        # "threshold" or "linear"

    # ═══ Vol Scaling ═══
    vol_target: 0.15               # 年化波動率目標

  symbol_overrides: {}

backtest:
  initial_cash: 10000
  fee_bps: 5
  slippage_bps: 3
  trade_on: "next_open"
  validate_data: true
  clean_data: true

  funding_rate:
    enabled: true
    default_rate_8h: 0.0001
    use_historical: true

  slippage_model:
    enabled: false

risk:
  max_drawdown_pct: 0.30

position_sizing:
  method: "fixed"
  position_pct: 1.0

portfolio:
  cash_reserve: 0
  allocation:
    BTCUSDT: 0.45
    ETHUSDT: 0.45
    SOLUSDT: 0.35
    BNBUSDT: 0.35
    DOGEUSDT: 0.35
    ADAUSDT: 0.35
    AVAXUSDT: 0.35
    LINKUSDT: 0.35

notification:
  telegram_bot_token: ${FUTURES_TELEGRAM_BOT_TOKEN}
  telegram_chat_id: ${FUTURES_TELEGRAM_CHAT_ID}
  prefix: "🔬 [RESEARCH-XSMOM]"
  enabled: true

output:
  report_dir: "./reports"
