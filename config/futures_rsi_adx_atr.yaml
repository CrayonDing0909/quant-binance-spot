# 合約策略配置範例：RSI + ADX + ATR
# 
# 與現貨版本 (rsi_adx_atr.yaml) 使用相同策略，但支援做空
# 
# 使用方式：
#   python scripts/download_data.py -c config/futures_rsi_adx_atr.yaml
#   python scripts/run_backtest.py -c config/futures_rsi_adx_atr.yaml
#   python scripts/run_live.py -c config/futures_rsi_adx_atr.yaml --paper

market:
  symbols: ["ETHUSDT", "SOLUSDT"]
  interval: "1h"
  start: "2022-01-01"
  end: null
  market_type: "futures"  # 使用合約 API

# 合約專屬配置
futures:
  leverage: 5             # 槓桿倍數（建議 1-5 倍）
  margin_type: "ISOLATED" # ISOLATED（逐倉）或 CROSSED（全倉）
  position_mode: "ONE_WAY" # ONE_WAY（單向）或 HEDGE（雙向）
  direction: "both"       # 交易方向: "both"=多空都做, "long_only"=只做多, "short_only"=只做空

strategy:
  name: "rsi_adx_atr"
  params:
    # 🌟 RSI 模式：dynamic (解決 Alpha Decay)
    rsi_mode: "dynamic"   # "static" or "dynamic"
    rsi_period: 10
    
    # Dynamic RSI 參數 (Rolling Percentile)
    rsi_lookback_days: 14  # 滾動窗口天數
    rsi_quantile_low: 0.10 # 買入閾值 (10th percentile)
    rsi_quantile_high: 0.90 # 賣出閾值 (90th percentile)
    
    # 靜態參數備份 (如果 mode=static)
    oversold: 30
    overbought: 80
    
    # ADX 過濾器
    min_adx: 15              # 做多 ADX 門檻（保持寬鬆，回調入場不需要強趨勢）
    short_min_adx: 20        # 做空 ADX 門檻（加密多頭偏差，空頭要求更強趨勢確認）
    adx_period: 14
    
    # 💰 Funding Rate 過濾器 (新功能)
    # 過濾擁擠交易：當 funding rate 極高時不做多，極低時不做空
    use_funding_filter: true
    fr_max_pos: 0.0002     # 0.02% (Longs paying Shorts)
    fr_max_neg: -0.0002    # -0.02% (Shorts paying Longs)
    
    # 🛡️ 風控優化 (基於熱力圖掃描結果)
    # 最佳平衡點：SL 2.0 / CD 3
    # 既能捕捉利潤 (比 2.5/5 更好)，又不像 1.5/1 那樣過於激進
    stop_loss_atr: 2.0     # 基準 SL 倍數（adaptive_sl 啟用時作為 fallback）
    take_profit_atr: null   # null = 不設固定止盈，靠 RSI 信號出場
    atr_period: 14
    
    # 🆕 自適應止損（Adaptive SL）
    # 根據 Efficiency Ratio 動態調整 SL 寬度：
    #   震盪市（ER 低）→ 放寬 SL（扛過噪聲）
    #   趨勢市（ER 高）→ 收緊 SL（保護利潤）
    # 回測結果（2025-2026）：MDD 12.1%→10.7%（BTC），13.8%→10.4%（ETH）
    adaptive_sl: true
    adaptive_sl_er_period: 10   # ER 計算週期
    er_sl_min: 1.5              # ER=1（趨勢）→ SL = 1.5x ATR（最窄）
    er_sl_max: 2.5              # ER=0（震盪）→ SL = 2.5x ATR（最寬）
    
    # 冷卻期 & 最小持倉
    cooldown_bars: 3       # 止損後等待 3 小時再入場
    min_hold_bars: 2       # 入場後至少持倉 2h（SL 不受限），防止 RSI 震盪快進快出
    
    # Time-of-Day 過濾（已驗證無效，不啟用）
    # 原理：封鎖 UTC 09/12 等低品質時段的新開倉
    # 結果：Sharpe 1.50→1.30，三幣全面下降
    # 原因：加密 24/7 無明顯 session 邊界；封鎖時段也擋掉好交易
    # blocked_hours: [9, 12]  # 保留備用，設為 null 或刪除此行即可停用
    
    # 🆕 波動率過濾器（過濾低波動磨耗）
    # ATR/Price < 0.005 時不開倉 → 避免震盪市頻繁交易
    min_atr_ratio: 0.005
    vol_filter_mode: "absolute"    # "absolute" or "percentile"
    
    # 🆕 多時間框架趨勢過濾（軟模式）
    # 4h EMA20/50 判斷大趨勢，用連續權重而非二元閘門
    # soft 模式：順趨勢全倉、逆趨勢半倉、無趨勢 75%
    htf_interval: "4h"
    htf_mode: "soft"               # "soft"（推薦）或 "hard"（激進）
    htf_ema_fast: 20
    htf_ema_slow: 50
    htf_align_weight: 1.0          # 順趨勢權重
    htf_counter_weight: 0.5        # 逆趨勢權重
    htf_neutral_weight: 0.75       # 無趨勢權重

  # 移除舊的靜態閾值覆寫，使用統一的動態邏輯
  symbol_overrides: {}

backtest:
  initial_cash: 10000
  fee_bps: 4              # 合約手續費較低（Maker 0.02%, Taker 0.04%）
  slippage_bps: 3         # 固定滑點（slippage_model.enabled=true 時被覆蓋）
  trade_on: "next_open"
  validate_data: true
  clean_data: true

  # 🔴 Funding Rate 成本模型（永續合約持倉成本）
  # 每 8 小時結算，多頭在 rate>0 時付費，空頭收費
  # 開啟後回測會顯示「調整後績效」，更貼近真實收益
  funding_rate:
    enabled: true
    default_rate_8h: 0.0001   # 0.01% per 8h（無歷史資料時的預設值）
    use_historical: true      # 嘗試載入歷史 funding rate（需先 download_data.py --funding-rate）

  # 🔴 Volume-based 滑點模型（Square-Root Market Impact）
  # slippage = base_spread + k × (trade_value / ADV)^0.5
  # 開啟後取代固定 slippage_bps，大資金時滑點會顯著增加
  slippage_model:
    enabled: true
    base_bps: 2               # 最低買賣價差 (bps)
    impact_coefficient: 0.1   # 衝擊係數 k（0.05~0.20，越大=流動性越差）
    impact_power: 0.5         # 衝擊指數（0.5 = 平方根模型，學術標準）
    adv_lookback: 20          # 計算平均成交量的回看 bar 數
    participation_rate: 0.10  # 市場佔比上限（10%）

# 風控（合約建議更保守）
risk:
  max_drawdown_pct: 0.65  # 65% 熔斷（5x 槓桿下 1x MDD≈14% → 5x≈57%，留緩衝）

# 倉位計算
# 曝險由 portfolio.allocation 統一控制，不再額外做 vol scaling。
# 原 "volatility" 模式在實盤從未生效（VolatilityPositionSizer 未收到 returns），
# 且與回測不一致，已改為 "fixed"。
position_sizing:
  method: "fixed"              # "fixed" or "volatility" or "kelly"
  position_pct: 1.0            # method=fixed 時使用（× portfolio.allocation 權重）
  kelly_fraction: 0.25         # Kelly 保守因子（保留備用）
  target_volatility: 0.50      # vol 模式用（已停用，留作參考）
  vol_lookback: 168            # vol 模式用（已停用，留作參考）

# 多幣種倉位分配
# ⚠️ 嚴格回測教訓（含 Funding Rate + Volume Slippage）：
#   - 簡化版（無 FR/滑點）會嚴重高估收益 → 「快樂表」
#   - 只有 ETH/SOL 在嚴格回測下存活（流動性夠大）
#   - AVAX/LINK/XRP/BNB 在嚴格版全部爆虧（流動性不足 + funding 成本）
#   - BTC 嚴格版 Sharpe 0.21，勉強打平，不值得佔用資金
#
# 嚴格版結果（2022-2026）：
#   ETH: Return +106%, Sharpe 0.65, 3/4 年正
#   SOL: Return +890%, Sharpe 1.30, 4/4 年正
#   組合: Return +436%, Sharpe 1.32, MDD 39.8%
portfolio:
  cash_reserve: 0
  allocation:
    ETHUSDT: 1.00         # ETH 穩定（嚴格版 Sharpe 0.65, 3/4 年正）
    SOLUSDT: 1.00         # SOL 最強（嚴格版 Sharpe 1.30, 4/4 年正）
    # 總曝險 200%（2x），5x 槓桿下保證金用 40%
    # 只保留流動性最好的兩幣，避免小幣種滑點磨損

# 實盤專屬配置
live:
  kline_cache: true           # 增量 K 線快取（策略從 bar 0 跑到最新，與回測一致）
  flip_confirmation: false    # 方向切換 2-tick 確認（快取模式下不需要）
  prefer_limit_order: true    # 🆕 優先用限價單（Maker fee 0.02% vs Taker 0.04%，省一半手續費）
  limit_order_timeout_s: 10   # 限價單等待秒數（超時自動改市價單確保成交）

# Telegram 通知（使用獨立的 Bot）
notification:
  telegram_bot_token: ${FUTURES_TELEGRAM_BOT_TOKEN}
  telegram_chat_id: ${FUTURES_TELEGRAM_CHAT_ID}
  prefix: "🔴 [FUTURES]"
  enabled: true

output:
  report_dir: "./reports"
