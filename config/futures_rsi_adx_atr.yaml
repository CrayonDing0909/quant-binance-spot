# 合約策略配置範例：RSI + ADX + ATR
# 
# 與現貨版本 (rsi_adx_atr.yaml) 使用相同策略，但支援做空
# 
# 使用方式：
#   python scripts/download_data.py -c config/futures_rsi_adx_atr.yaml
#   python scripts/run_backtest.py -c config/futures_rsi_adx_atr.yaml
#   python scripts/run_live.py -c config/futures_rsi_adx_atr.yaml --paper

market:
  symbols: ["BTCUSDT", "ETHUSDT"]
  interval: "1h"
  start: "2022-01-01"
  end: null
  market_type: "futures"  # 使用合約 API

# 合約專屬配置
futures:
  leverage: 5             # 槓桿倍數（建議 1-5 倍）
  margin_type: "ISOLATED" # ISOLATED（逐倉）或 CROSSED（全倉）
  position_mode: "ONE_WAY" # ONE_WAY（單向）或 HEDGE（雙向）
  direction: "both"       # 交易方向: "both"=多空都做, "long_only"=只做多, "short_only"=只做空

strategy:
  name: "rsi_adx_atr"
  params:
    # RSI 參數
    rsi_period: 10
    oversold: 30           # RSI < 30 → 做多
    overbought: 70         # RSI > 70 → 做空（合約專用）
    
    # ADX 過濾器
    min_adx: 15
    adx_period: 14
    
    # ATR 止損止盈
    stop_loss_atr: 1.5     # 止損 = 1.5 ATR
    take_profit_atr: null   # null = 不設固定止盈，靠 RSI 信號出場（回測最優）
    atr_period: 14
    
    # 冷卻期
    cooldown_bars: 1

  # 幣種專屬參數覆寫
  symbol_overrides:
    BTCUSDT:
      oversold: 30
      overbought: 70
    ETHUSDT:
      oversold: 35
      overbought: 65

backtest:
  initial_cash: 10000
  fee_bps: 4              # 合約手續費較低（Maker 0.02%, Taker 0.04%）
  slippage_bps: 3
  trade_on: "next_open"
  validate_data: true
  clean_data: true

# 風控（合約建議更保守）
risk:
  max_drawdown_pct: 0.65  # 65% 熔斷（5x 槓桿下 1x MDD≈14% → 5x≈57%，留緩衝）

# 倉位計算
position_sizing:
  method: "fixed"
  position_pct: 1.0
  kelly_fraction: 0.25

# 多幣種倉位分配
portfolio:
  cash_reserve: 0
  allocation:
    BTCUSDT: 1.00         # 100% 權益
    ETHUSDT: 1.00         # 100% 權益
    # 總曝險 200%（2x），5x 槓桿下保證金用 40%，MDD 預估 ~27%

# Telegram 通知（使用獨立的 Bot）
notification:
  telegram_bot_token: ${FUTURES_TELEGRAM_BOT_TOKEN}
  telegram_chat_id: ${FUTURES_TELEGRAM_CHAT_ID}
  prefix: "🔴 [FUTURES]"
  enabled: true

output:
  report_dir: "./reports"
