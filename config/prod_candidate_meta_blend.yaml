# ═══════════════════════════════════════════════════════════════
# PRODUCTION CANDIDATE — Meta-Blend 8-Symbol (R3C × tsmom_carry_v2)
# ═══════════════════════════════════════════════════════════════
# Created: 2026-02-25
# Origin: config/research_meta_blend.yaml (validated research config)
# Lineage: tsmom_carry_v2 (R4 research) → meta_blend (R3C BTC integration)
#
# STRATEGY SUMMARY:
#   meta_blend 混合多個子策略信號，per-symbol 路由：
#     BTC:   breakout_vol_atr(30%) + tsmom_carry_v2 btc_enhanced(70%)
#     ETH:   100% tsmom_carry_v2 eth_enhanced (OI/FR/Basis)
#     SOL/AVAX: 100% tsmom_carry_v2 tsmom_heavy (70/30)
#     BNB/DOGE/ADA/LINK: 100% tsmom_carry_v2 default (50/50)
#
#   Removed symbols:
#     LTCUSDT — persistent negative OOS IC, no alpha
#     XRPUSDT — Sharpe 0.046, WFA OOS+ 40%, fails 2x cost
#
# VALIDATION RESULTS (2026-02-25):
#   Portfolio Sharpe:  1.201 (vs pure V2: 1.127, vs R3C: 1.013)
#   Total Return:      +43.6%
#   Max Drawdown:      -5.0%
#   2x Cost Sharpe:    0.660 → PASS
#   WFA OOS Positive:  88% (avg OOS Sharpe +0.85)
#   BTC Blend SR:      1.177 (vs R3C breakout: 1.182, V2: 0.894)
#
# KEY DIFFERENCES vs prod_live_R3C_E3.yaml:
#   - 8 symbols (was 10) — removed LTC (quarantined) and XRP (no alpha)
#   - Allocation sum = 1.0 (was 3.13) — conservative, no allocation leverage
#   - meta_blend handles per-symbol routing (no ensemble block needed)
#   - tsmom_carry_v2 adds carry confirmation to reduce MDD
#   - breakout_vol_atr retained for BTC (30% blend weight)
#
# RISK AUDIT (2026-02-25):
#   Monte Carlo Bootstrap:   5th pct Return > 0 → PASS
#   Trade-Order Shuffle:     MDD within bounds → PASS
#   Cost Perturbation:       Sharpe > 0 at 2x costs → PASS
#   Kelly Fraction:          Recommended 0.25 Kelly
#   Portfolio VaR (95%):     Within 40% MDD limit
#   Concentration (HHI):     0.128 (well-diversified)
#   Verdict: APPROVED (CONDITIONAL — paper trade 1-2 weeks first)
#
# DEPLOYMENT:
#   1. Paper trade: run_live.py -c config/prod_candidate_meta_blend.yaml --paper
#   2. Monitor 1-2 weeks for signal consistency
#   3. If MDD < 10% and signals match backtest → promote to prod_live
#   4. If live MDD exceeds 10%, pause and review
#
# ROLLBACK:
#   Stop this config → restart with prod_live_R3C_E3.yaml (existing prod)
# ═══════════════════════════════════════════════════════════════

market:
  symbols:
    - BTCUSDT
    - ETHUSDT
    - SOLUSDT
    - BNBUSDT
    - DOGEUSDT
    - ADAUSDT
    - AVAXUSDT
    - LINKUSDT
  interval: "1h"
  start: "2022-01-01"
  end: null
  market_type: "futures"

futures:
  leverage: 3
  margin_type: "ISOLATED"
  position_mode: "ONE_WAY"
  direction: "both"

strategy:
  name: "meta_blend"
  params:
    # ── Default sub-strategy combo (BNB, DOGE, ADA, LINK) ──
    # 100% tsmom_carry_v2 — carry confirmation reduces MDD
    sub_strategies:
      - name: "tsmom_carry_v2"
        weight: 1.0
        params:
          combination_mode: "confirmatory"
          tier: "default"
          tsmom_lookback: 168
          tsmom_vol_target: 0.15
          tsmom_ema_fast: 20
          tsmom_ema_slow: 50
          tsmom_agree_weight: 1.0
          tsmom_disagree_weight: 0.3
          basis_ema_fast: 24
          basis_ema_slow: 168
          basis_tanh_scale: 50.0
          fr_rolling_window: 72
          oi_z_window: 168
          basis_carry_fr_weight: 0.7
          basis_carry_basis_weight: 0.3
          w_tsmom: 0.50
          w_basis_carry: 0.50
          carry_agree_scale: 1.0
          carry_disagree_scale: 0.7
          carry_neutral_scale: 0.85
          carry_confirm_smoothing: 24
          composite_ema: 12
          position_step: 0.1
          min_change_threshold: 0.05

  symbol_overrides:
    # ── BTC: breakout_vol_atr(30%) + tsmom_carry_v2(70%) ──
    # breakout_vol_atr Sharpe 1.18 vs V2 0.89 on BTC; corr = 0.60
    BTCUSDT:
      sub_strategies:
        - name: "breakout_vol_atr"
          weight: 0.30
          params:
            channel_period: 336
            channel_multiplier: 2.5
            atr_period: 14
            vol_fast_period: 14
            vol_slow_period: 72
            expansion_ratio: 1.1
            fake_breakout_bars: 6
            min_hold_bars: 24
            trail_exit_mid: true
            stop_loss_atr: 3.5
            max_holding_bars: 72
            cooldown_bars: 24
            vol_scale_enabled: true
            vol_scale_target: 99.0
            vol_scale_lookback: 168
            vol_scale_floor: 0.7
            vol_scale_cap: 0.7
            volume_confirm_enabled: true
            volume_confirm_period: 20
            volume_confirm_ratio: 1.5
        - name: "tsmom_carry_v2"
          weight: 0.70
          params:
            combination_mode: "confirmatory"
            tier: "btc_enhanced"
            tsmom_lookback: 720
            tsmom_ema_fast: 30
            tsmom_ema_slow: 100
            tsmom_vol_target: 0.15
            tsmom_agree_weight: 1.0
            tsmom_disagree_weight: 0.3
            basis_ema_fast: 24
            basis_ema_slow: 168
            basis_tanh_scale: 50.0
            carry_agree_scale: 1.0
            carry_disagree_scale: 0.6
            carry_neutral_scale: 0.85
            carry_confirm_smoothing: 24
            composite_ema: 12
            position_step: 0.1
            min_change_threshold: 0.05

    # ── ETH: 100% tsmom_carry_v2 (eth_enhanced with OI/FR/Basis) ──
    ETHUSDT:
      sub_strategies:
        - name: "tsmom_carry_v2"
          weight: 1.0
          params:
            combination_mode: "confirmatory"
            tier: "eth_enhanced"
            tsmom_lookback: 168
            tsmom_vol_target: 0.15
            tsmom_ema_fast: 20
            tsmom_ema_slow: 50
            tsmom_agree_weight: 1.0
            tsmom_disagree_weight: 0.3
            w_tsmom: 0.40
            w_oi: 0.25
            w_fr: 0.15
            w_basis: 0.20
            basis_ema_fast: 24
            basis_ema_slow: 168
            basis_tanh_scale: 50.0
            fr_rolling_window: 72
            oi_z_window: 168
            carry_agree_scale: 1.0
            carry_disagree_scale: 0.7
            carry_neutral_scale: 0.85
            carry_confirm_smoothing: 24
            composite_ema: 12
            position_step: 0.1
            min_change_threshold: 0.05

    # ── SOL: 100% tsmom_carry_v2 (tsmom_heavy: TSMOM 70% + BasisCarry 30%) ──
    SOLUSDT:
      sub_strategies:
        - name: "tsmom_carry_v2"
          weight: 1.0
          params:
            combination_mode: "confirmatory"
            tier: "tsmom_heavy"
            tsmom_lookback: 168
            tsmom_vol_target: 0.15
            tsmom_ema_fast: 20
            tsmom_ema_slow: 50
            tsmom_agree_weight: 1.0
            tsmom_disagree_weight: 0.3
            w_tsmom: 0.70
            w_basis_carry: 0.30
            basis_ema_fast: 24
            basis_ema_slow: 168
            basis_tanh_scale: 50.0
            fr_rolling_window: 72
            basis_carry_fr_weight: 0.7
            basis_carry_basis_weight: 0.3
            carry_agree_scale: 1.0
            carry_disagree_scale: 0.7
            carry_neutral_scale: 0.85
            carry_confirm_smoothing: 24
            composite_ema: 12
            position_step: 0.1
            min_change_threshold: 0.05

    # ── AVAX: 100% tsmom_carry_v2 (tsmom_heavy: TSMOM 70% + BasisCarry 30%) ──
    AVAXUSDT:
      sub_strategies:
        - name: "tsmom_carry_v2"
          weight: 1.0
          params:
            combination_mode: "confirmatory"
            tier: "tsmom_heavy"
            tsmom_lookback: 168
            tsmom_vol_target: 0.15
            tsmom_ema_fast: 20
            tsmom_ema_slow: 50
            tsmom_agree_weight: 1.0
            tsmom_disagree_weight: 0.3
            w_tsmom: 0.70
            w_basis_carry: 0.30
            basis_ema_fast: 24
            basis_ema_slow: 168
            basis_tanh_scale: 50.0
            fr_rolling_window: 72
            basis_carry_fr_weight: 0.7
            basis_carry_basis_weight: 0.3
            carry_agree_scale: 1.0
            carry_disagree_scale: 0.7
            carry_neutral_scale: 0.85
            carry_confirm_smoothing: 24
            composite_ema: 12
            position_step: 0.1
            min_change_threshold: 0.05

  # No overlay needed — carry confirmation replaces oi_vol overlay
  overlay:
    enabled: false

backtest:
  initial_cash: 10000
  fee_bps: 5
  slippage_bps: 3
  trade_on: "next_open"
  validate_data: true
  clean_data: true
  funding_rate:
    enabled: true
    default_rate_8h: 0.0001
    use_historical: true
  slippage_model:
    enabled: false

risk:
  max_drawdown_pct: 0.40

position_sizing:
  method: "fixed"
  position_pct: 1.0

# ── Portfolio allocation: 8-symbol (LTC + XRP removed) ──
# BTC/ETH overweight (15%) — strongest carry alpha + BTC breakout
# Others equal-weight (~11.67%)
# Sum = 1.0 (no allocation leverage; futures leverage = 3x handles sizing)
portfolio:
  cash_reserve: 0
  allocation:
    BTCUSDT: 0.1500
    ETHUSDT: 0.1500
    SOLUSDT: 0.1167
    BNBUSDT: 0.1167
    DOGEUSDT: 0.1167
    ADAUSDT: 0.1167
    AVAXUSDT: 0.1166
    LINKUSDT: 0.1166

live:
  kline_cache: true
  flip_confirmation: false
  prefer_limit_order: true
  limit_order_timeout_s: 10

  # ── Rebalance Band: 3% threshold ──
  # Skip rebalance when |target - current| < 3% and same direction
  rebalance_band:
    enabled: true
    threshold_pct: 0.03
    apply_on_same_direction_only: true

  # ── Symbol Governance: automated edge monitoring ──
  symbol_governance:
    enabled: true
    review_frequency: "weekly"
    warmup_days: 14
    quarantine_min_days: 14
    consecutive_reviews_for_quarantine: 2
    consecutive_reviews_for_recovery: 2
    artifacts_dir: "reports/symbol_governance"

    thresholds:
      edge_sharpe_deweight: 0.3
      edge_sharpe_quarantine: 0.0
      edge_sharpe_recover_active: 0.8
      edge_sharpe_recover_from_quarantine: 0.5
      edge_per_turnover_min: 0.0
      slippage_ratio_deweight: 1.5
      slippage_ratio_quarantine: 1.8
      slippage_ratio_recover_active: 1.2
      slippage_ratio_recover_from_quarantine: 1.3
      consistency_min: 99.0
      consistency_recover: 99.5
      missed_signals_quarantine: 5.0
      dd_quarantine_pct: 25.0

    weights:
      active_multiplier: 1.0
      deweight_multiplier: 0.5
      quarantine_multiplier: 0.0
      min_weight: 0.03
      max_weight: 0.20

  watchdog:
    enabled: true
    interval_sec: 300
    alert_cooldown_sec: 1800
    startup_grace_sec: 300
    runner_ready_grace_sec: 180
    heartbeat_warn_sec: 90
    heartbeat_critical_sec: 180
    ws_msg_warn_sec: 180
    ws_msg_critical_sec: 300
    data_warn_age_sec:
      "1h": 7200
      "5m": 1800
    data_critical_age_sec:
      "1h": 14400
      "5m": 3600
    error_window_minutes: 15
    error_warn_count: 5
    error_critical_count: 12

notification:
  telegram_bot_token: ${FUTURES_TELEGRAM_BOT_TOKEN}
  telegram_chat_id: ${FUTURES_TELEGRAM_CHAT_ID}
  prefix: "🔬 [CANDIDATE-MetaBlend-8S]"
  enabled: true

output:
  report_dir: "./reports"
