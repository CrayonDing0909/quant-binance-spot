---
alwaysApply: true
---
# Recent Changes (Rolling Log)

Agent 在每次 session 結束時更新此檔案。保留最近 30 天的條目，超過的刪除。
新 Chat 開啟時 agent 可從此處了解最近發生的所有變更。

---

## 2026-02-26

### 多時間框架 + 衍生品數據擴展（Phase 0–4 完整實作）
- **Phase 0a**: 新增 `scripts/fetch_derivatives_data.py` — 衍生品數據（LSR、Taker Vol、CVD）下載腳本
- **Phase 0b**: 新增 `scripts/fetch_liquidation_data.py` — 爆倉數據下載腳本（Coinglass API）
- **Phase 0c**: 修改 `scripts/download_data.py` — 支援多 interval 批次下載（逗號分隔，如 `--interval 1h,4h,1d`）
- **Phase 0d**: 新增 `scripts/fetch_onchain_data.py` — 鏈上數據探索腳本（DeFi Llama TVL、穩定幣市值等）
- **Phase 0e**: 更新 `.cursor/agents/alpha-researcher.md` — 新增多時間框架數據目錄、衍生品數據源、Microstructure/Multi-TF 原型分析模板
- **Phase 2A**: 修改 `src/qtrade/config.py` — `MarketConfig` 新增 `auxiliary_intervals` 欄位
- **Phase 2B**: 新增/更新數據模組：
  - `src/qtrade/data/long_short_ratio.py`, `taker_volume.py`, `liquidation.py` — 正式衍生品數據模組
  - `src/qtrade/data/onchain.py` — 鏈上數據模組（DeFi Llama 佔位實作）
  - `src/qtrade/data/__init__.py` — 匯出新模組
- **Phase 2C**: 新增 `src/qtrade/data/multi_tf_loader.py` — `MultiTFLoader` 多時間框架數據載入器（因果對齊）
- **Phase 2D**: 修改 `src/qtrade/strategy/base.py` — `StrategyContext` 新增 `auxiliary_data`, `derivatives_data` 欄位 + 便利方法
- **Phase 3A**: 新增 `src/qtrade/strategy/multi_tf_resonance_strategy.py` — 多時間框架共振策略
- **Phase 3B**: 新增 `src/qtrade/strategy/derivatives_enhanced_strategy.py` — 衍生品增強策略（crowding_contrarian、cvd_divergence、liq_cascade_v2）
- **Phase 3C**: 新增 `src/qtrade/strategy/overlays/derivatives_micro_overlay.py` — 衍生品微觀結構增強 overlay
- **Phase 4A**: 修改 `src/qtrade/live/websocket_runner.py` — multi-interval WS 訂閱 + auxiliary K 線快取 + 策略注入
- **Phase 4A**: 修改 `src/qtrade/live/base_runner.py` — 衍生品數據快取（_derivatives_cache，30 分鐘刷新）+ 策略注入
- 修改 `src/qtrade/strategy/__init__.py` — 註冊新策略
- 修改 `.cursorignore` — 修正 `data/` 過廣問題，改為 `data/*` 以允許 `src/qtrade/data/` 存取
- 重新生成 `docs/CLI_REFERENCE.md`
- 全部 334 測試通過

### 多時間框架 + 衍生品數據擴展（Phase 2E–4C 補完）
- **Phase 2E**: 修改 `scripts/download_data.py` — 新增 `--derivatives` flag，自動下載 LSR + Taker Vol + CVD 到 `data/binance/futures/derivatives/`
- **Phase 2F**: 修改 `.cursor/agents/devops.md` — Cron 區塊新增衍生品定期下載（每日 03:00 UTC）、數據存放路徑新增衍生品/liquidation 路徑
- **Phase 3D**: 新增 `src/qtrade/strategy/low_freq_portfolio_strategy.py` — 低頻組合層策略（risk regime + 動量輪動 + 鏈上 regime）
- **Phase 4B**: 修改 `src/qtrade/live/base_runner.py` — 新增 `_start_derivatives_bg_refresh()` + `_poll_derivatives_api()` 後台線程，定期從 Binance API 拉取 LSR/Taker Vol 並寫入 parquet
- **Phase 4B**: 修改 `src/qtrade/live/websocket_runner.py` — `run()` 方法啟動衍生品 API 後台輪詢線程
- **Phase 4C**: 新增 `src/qtrade/data/order_book.py` — Order Book Depth 數據模組（`OrderBookSnapshot`, `OrderBookCache`, `compute_imbalance`, `compute_depth_profile`, `parse_depth_message`）
- 修改 `src/qtrade/strategy/__init__.py` — 註冊 `low_freq_portfolio` 策略
- 修改 `src/qtrade/data/__init__.py` — 匯出 `order_book` 模組
- 重新生成 `docs/CLI_REFERENCE.md`
- 全部 341 測試通過

### 文獻參考庫獨立化 + alpha-researcher.md 清理
- 新增 `docs/RESEARCH_LITERATURE.md` — 學術文獻參考庫獨立為 living doc
  - 按 7 個策略原型分類（Trend / MR / Carry / Vol / Event / Multi-TF / Microstructure）
  - 每個原型含：經典文獻、加密貨幣專屬研究、關鍵實踐洞察
  - 新增跨原型通用文獻（市場總論、量化方法論、組合管理）
  - 補齊原本缺失的 Event-Driven / Multi-TF / Microstructure 文獻區塊
  - 新增「如何貢獻新文獻」指引，agent 可在研究過程中自行添加
- 修改 `.cursor/agents/alpha-researcher.md` — 重大清理
  - 移除 ~500 個重複的 front matter block（2000+ 行垃圾 → 單一 front matter）
  - 文獻區塊替換為指向 `docs/RESEARCH_LITERATURE.md` 的引用
  - 自我檢查清單新增「文獻維護」項目
  - 關鍵參考文件列表新增 `RESEARCH_LITERATURE.md`
- 修改 `.cursor/rules/hygiene.mdc` — Living docs 表新增 `RESEARCH_LITERATURE.md`

## 2026-02-25

### Satellite 策略探索歸檔 + Alpha Researcher 原型框架升級
- 修改 `.cursor/agents/alpha-researcher.md` — 新增 3 大 section：
  1. **策略原型分類與評估矩陣**（Step 1 分類、Step 2 專屬分析、Step 3 Kill Criteria）
  2. **成本敏感度框架**（按原型估算成本侵蝕，MR 鐵律：先模擬 gross PnL）
  3. **學術文獻參考庫**（Trend/MR/Carry/Vol 各含經典 + 加密專屬文獻）
  - 更新 Strategy Proposal 模板：新增 Section 0 Archetype Classification、Section 6 結構化風險欄位
  - 更新自我檢查清單：新增 6 項原型專屬檢查（MR gross PnL、成本侵蝕、正偏態影響）
- Archetype-Aware 分析結果（5 個方向）：
  - 🔴 Pure BB MR — Kill Criteria 3 秒觸發（8/8 幣種 gross PnL < 0）
  - 🟡 Vol Squeeze — 4/8 幣種通過，edge 勉強覆蓋成本，不建議單獨投入
  - 🔴 FR Carry — Kill Criteria 觸發（SOL/BNB 2yr FR < 0），portfolio SR=-0.63
  - 🔴 OI Spike — 通過 Kill Criteria 但 hit rate ≈ 50%（無 alpha）
  - ✅ OI Liq Bounce — 已是最佳 satellite（SR=2.49, corr≈0.01）
- 歸檔研究檔案：
  - `config/research_tsmom_mr_composite.yaml` → `config/archive/`
  - `notebooks/research/20260224_vol_squeeze_exploration.ipynb` → `notebooks/research/archive/`
- 重新生成 `docs/CLI_REFERENCE.md`
- 結論：OI Liq Bounce 是當前框架唯一可行 satellite，等 paper trading 畢業（≥ 2026-03-11）

### 任務路由機制簡化
- 修改 `docs/CURSOR_WORKFLOW.md` — 速查表分為三大類（策略開發/部署維運/維護改進），新增「維護 & 改進」路由（改進腳本、清理配置、流程改善、agent 定義更新），新增「不確定找誰？」預設規則
- 修改 `.cursor/commands/config-freeze.md` — 新增第 5 步「掃描舊配置引用」（grep 掃描 + 更新過時引用），防止策略替換後殘留過時引用

### 架構審計 + 流程補齊 + 過時引用修正
- 新增 `.cursor/commands/trade-review.md` — `/trade-review` 快捷指令（Risk Manager 每週交易復盤）
- 修改 `.cursor/commands/risk-review.md` — 更新為 Meta-Blend 配置引用，新增交易復盤步驟
- 修改 `.cursor/commands/risk-action.md` — 更新 baseline 配置引用為 Meta-Blend
- 修改 `docs/CURSOR_WORKFLOW.md` — Mode C 新增 `/trade-review` 步驟、Slash Commands 表新增 `/trade-review` 和 `/eod`、tmux 引用更新為 `meta_blend_live`
- 修改 `.cursor/agents/risk-manager.md` — 每週檢查新增交易復盤步驟（Step 4）、新增復盤相關警戒指標、配置引用更新為 Meta-Blend
- 修改 `.cursor/agents/quant-developer.md` — 生產配置引用更新為 Meta-Blend
- 修改 `.cursor/agents/alpha-researcher.md` — 數據下載配置引用更新為 Meta-Blend
- 修改 `.cursor/rules/strategy-dev.mdc` — 生產配置引用更新為泛用寫法
- 修改 `.cursor/rules/live-trading.mdc` — Deployment 區塊更新為 Meta-Blend + OI Liq Bounce 雙策略、新增 `trade_review.py` 指令
- 重新生成 `docs/CLI_REFERENCE.md`

### 回測數據延伸 + 交易復盤工具
- 延伸所有 8 幣種 Kline 數據至 2020 年起（binance_vision 來源），覆蓋 COVID crash + 2021 bull run
  - BTC: 2019-09, ETH: 2019-11, SOL: 2020-09, BNB: 2020-02, DOGE: 2020-07, ADA: 2020-01, AVAX: 2020-09, LINK: 2020-01
  - 從 ~3 年擴展到 5.4-6.5 年的歷史數據
- 延伸 Funding Rate 數據至 2020 年起（5 幣種重新下載完整 FR）
- 延伸數據回測驗證：Portfolio SR 2.57, MDD -4.89%, Calmar 5.10（比 3 年數據更強）
- 新增 `scripts/trade_review.py` — 交易復盤工具（4 個診斷 section）：
  1. 交易摘要（勝率、PnL、per-symbol breakdown）
  2. 信號 vs 執行審計（方向匹配檢查）
  3. 市場環境分析（ADX/ATR regime 分類：趨勢/盤整/高波動）
  4. 回測重播比較（同期間重跑回測比對實盤 PnL）
  - 支援 `--with-replay`, `--symbol`, `--days`, `--output` JSON
- 重新生成 `docs/CLI_REFERENCE.md`

### 統一多策略 Telegram Bot（全新模組）
- 新增 `src/qtrade/monitor/multi_strategy_bot.py` — `MultiStrategyBot` 類別，獨立進程運行，支援多策略
  - 直連 Binance API 查詢帳戶狀態（無需 Runner 橋接）
  - 讀取各策略 `last_signals.json` 顯示信號
  - 命令重設計：`/dashboard`, `/status`, `/positions`, `/pnl`, `/signals`, `/risk`, `/health`, `/help`
  - 所有命令支援 inline buttons（時段切換、展開/收起、策略篩選）
  - 每日自動摘要（UTC 00:05）
  - 可配置告警系統（drawdown、circuit breaker、大額交易）
- 重構 `scripts/run_telegram_bot.py` — 支援多個 `-c config.yaml` 參數
- 修改 `src/qtrade/live/websocket_runner.py` — 新增 `_save_last_signals()` 方法，每次信號更新後寫 `last_signals.json`
- 修改 `scripts/run_live.py` — `--telegram-commands` 已棄用（印提示指向 `run_telegram_bot.py`）
- `scripts/run_websocket.py` 中 TelegramCommandBot 啟動早已移除（僅保留 TelegramNotifier）
- 更新 `devops.md` — 新增「統一 Telegram Bot 部署」section（架構圖、tmux 指令、環境變數）
- 重新生成 `docs/CLI_REFERENCE.md`

### Meta-Blend 生產部署（替換 R3C）
- Meta-Blend 8-Symbol 上線替換 R3C 10-Symbol（tmux: `meta_blend_live`）
- R3C 所有 10 個空頭倉位平倉（+20.34 USDT），tmux `r3c_e3_live` 已停止
- 配置 `config/prod_candidate_meta_blend.yaml` 用作生產（含 LINK tsmom_only 修正）
- Oracle Cloud cron 新增 Meta-Blend 數據下載（每 6h），R3C cron 保留供 rollback
- 更新 `project-overview.mdc`：Meta-Blend 為 Active Production，R3C 為 Retired
- 更新 `devops.md`：Meta-Blend 架構、Cron、rollback 說明

### FR 數據路徑修復 + LINK tier 調整
- 修復 `scripts/research_overlay_4way.py` 和 `scripts/research_strategy_blend.py` 的 FR 路徑 bug（`data/binance/binance/` 雙層目錄）
- LINKUSDT 從 `tsmom_carry_v2/default` 切換為 `tsmom_only`（SR +15.4%，carry IC 不穩定）
- 2x cost stress test 用修正路徑重跑並存檔

### Overlay 一致性審計與修正（4 項）
- 新增 `scripts/research_overlay_4way.py` — 4-Way 比較腳本（R3C ±overlay × MetaBlend ±overlay，8 幣種），結果寫入 `reports/research/overlay_4way/`
- 修改 `.cursor/rules/backtest.mdc` — 新增 Overlay Audit Rules（報告必須標註 overlay ON/OFF、比較需提供 with/without、ablation 數據要求）
- 修改 `config/prod_candidate_meta_blend.yaml` — 根據 ablation 數據啟用 vol_pause overlay（Sharpe +1.105），更新驗證數據（Portfolio SR 2.306, MDD -3.7%, Calmar 5.02）
- 修改 `src/qtrade/live/signal_generator.py` — 修復 live overlay OI 載入缺失（原本硬編碼 `oi_series=None`，改為與 backtest 一致的自動載入邏輯）
- 重新生成 `docs/CLI_REFERENCE.md`

### Rules / Commands / Workflow 一致性修正（8 處）
- 修改 `.cursor/rules/project-overview.mdc` — 更新 Handoff Rules（Developer 不跑 WFA、新增 config-freeze 步驟）、更新 Workflow 圖（插入 Developer config-freeze 節點）、更新 Agent Scope 欄位
- 修改 `.cursor/rules/backtest.mdc` — Validation Stack 區塊加入分工 note（Developer 只跑 --quick，完整驗證由 Researcher 執行）
- 修改 `.cursor/rules/strategy-dev.mdc` — Development Lifecycle Step 4 加入驗證分工說明
- 修改 `.cursor/commands/backtest.md` — 移除 WFA，改為回測 + --quick 驗證
- 修改 `.cursor/commands/research-audit.md` — 擴充為完整驗證 pipeline（新增 CPCV、Cost Stress、Delay Stress）
- 修改 `docs/CURSOR_WORKFLOW.md` — Mode A/B 加入 config-freeze 步驟、速查表移除 WFA、上線前 checklist 修正 config 前綴和 owner、文件產出路徑表修正生產配置前綴、Slash Commands 表加入 /config-freeze、Risk Manager Next Steps 更新
- 新增 `.cursor/commands/config-freeze.md` — 凍結研究配置為生產配置的快捷指令

### Agent 職責釐清（7 項修正）
- 修改 `.cursor/agents/quant-developer.md` — 釐清驗證分工（Developer 只跑 `--quick`，完整驗證交 Researcher）、新增「生產配置凍結」職責、新增「共用模組維護」職責、釐清假說確認 vs 定義的分工（Mode A/B）、新增數據下載步驟
- 修改 `.cursor/agents/quant-researcher.md` — 新增「驗證分工原則」（獨立重跑，不信任 Developer 數字）、新增「不修改 src/qtrade/ 程式碼」規則
- 修改 `.cursor/agents/alpha-researcher.md` — 新增「數據管理職責」分工、新增「初步驗證邊界」（可以做 IC 分析，不應做 vectorbt 回測）
- 修改 `.cursor/agents/risk-manager.md` — 新增「不修改 src/qtrade/ 程式碼」規則、新增「月度審查分工」（主導 MC+相關性+Kelly，異常交 Researcher 深入分析）
- 修改 `.cursor/agents/devops.md` — 新增「不建立生產配置」規則、新增「數據管理分工」（生產/持久化階段獨佔）
- 修改 `docs/STRATEGY_PORTFOLIO_GOVERNANCE.md` — 統一定期審查排程（月度由 Risk Manager 主導，Researcher 僅在 alpha decay 異常時介入）

### OI Liquidation Bounce 策略（完整生命週期）
- 新增 `src/qtrade/strategy/oi_liq_bounce_strategy.py` — OI 清算反彈策略 v4.2
- 新增 `config/research_oi_liq_bounce.yaml` — 5-symbol long-only (BTC/ETH/SOL/DOGE/AVAX)
- 通過完整驗證棧：DSR, WFA, Cost Stress, Delay Stress 全 PASS
- 通過風控審計：Monte Carlo 4/4 PASS, Portfolio Risk 3/3 PASS, APPROVED
- 更新 `project-overview.mdc` 和 `devops.md` 記錄候選狀態

### 策略組合管理框架（新工具鏈）
- 新增 `docs/STRATEGY_PORTFOLIO_GOVERNANCE.md` — 策略組合治理規範（納入/替換/退場標準）
- 新增 `scripts/compare_strategies.py` — 策略比較工具（邊際 Sharpe、相關性矩陣、最佳權重）
- 新增 `scripts/generate_blend_config.py` — meta_blend YAML 配置自動生成
- 修改 `scripts/run_portfolio_backtest.py` — 新增 `--multi-strategy` 多策略組合回測模式
- 重新生成 `docs/CLI_REFERENCE.md`

### 跨 Chat 知識同步機制
- 新增 `.cursor/rules/session-checklist.mdc` — session 結束自查 checklist
- 新增 `.cursor/rules/recent-changes.mdc` — 滾動變更日誌（本檔案）
- 更新 `project-overview.mdc` — 加入 meta_blend 候選、策略組合管理工具鏈
- 更新 `hygiene.mdc` — Living docs 表加入 STRATEGY_PORTFOLIO_GOVERNANCE.md

### OI Liq Bounce Paper Trading 部署
- **Bug Fix**: `BaseRunner._get_strategy_for_symbol()` 新增自動注入 `_data_dir`（修正 OI/FR 策略在 live 模式下拿不到輔助數據、返回全 0 信號的問題）
- 新增 `config/prod_live_oi_liq_bounce.yaml` — 套用 Risk Audit 條件（position_pct=0.50, circuit_breaker=10%, 1x leverage）
- Oracle Cloud 部署：tmux session `oi_liq_paper`，與 `r3c_e3_live` 平行運行
- Paper trading 起始日：2026-02-25，畢業門檻：≥ 2 weeks 無異常
- 更新 `devops.md` — OI Liq Bounce 狀態從「候選」更新為「Paper Trading 中 🟡」

### 多 Runner 數據管線修復（6 項）
- **P0 Fix**: `LiveWatchdog` 輸出目錄按策略名隔離（`reports/live_watchdog/{strategy_name}/`），修正多 runner 共用 PID 檔導致的假 critical 告警
- **P1 新增**: `LiveWatchdog._check_oi_freshness()` — OI parquet mtime 監控，warn=6h / critical=12h，`config/prod_live_oi_liq_bounce.yaml` 已啟用 `oi_freshness_enabled: true`
- **P1 Cron**: Oracle Cloud OI Binance API cron 從 `*/4` 改為 `*/2`（每 2 小時），減少 OI 空窗期
- **P2 新增**: `BaseRunner._oi_cache` — OI 記憶體快取（每 30 分鐘刷新），避免每次信號生成都讀 parquet
- **P3 新增**: `scripts/download_data.py --oi` — 統一數據管線，`oi_liq_bounce` 策略自動觸發 OI 下載+合併
- 更新 `devops.md` — 數據管理區塊重寫（統一指令、cron 列表、watchdog 路徑說明）

### 策略開發流程缺口審計與修復（7 個缺口，3 大區域）
- **G1+G2**: 修改 `docs/STRATEGY_DEV_PLAYBOOK_R2_1.md` — 新增 Stage D.5 Overlay Ablation（2×2 和 3-way ablation 協議）+ Section 7 驗證矩陣（V1-V12，12 項必檢 gate，指定負責人和通過標準）+ 更新 Production Promotion Checklist
- **G3**: 修改 `.cursor/agents/quant-developer.md` — 回測報告清單新增 overlay 狀態和 ablation 要求、自我檢查清單新增 overlay 一致性 + ablation 確認
- **G3**: 修改 `.cursor/agents/quant-researcher.md` — 驗證 Pipeline 新增第 8 步（Overlay Consistency Check）和第 9 步（Pre-Deploy Consistency Check via `validate_live_consistency.py`）、判決標準加註一致性 gate 必須通過
- **G4（最關鍵）**: 修改 `scripts/validate_live_consistency.py` — 新增 `check_overlay_consistency` 檢查項（overlay mode/params 一致性、OI 數據可用性、BaseRunner _data_dir 注入驗證）
- **G4**: 修改 `src/qtrade/validation/consistency.py` — `ConsistencyValidator` 新增 `overlay_cfg` + `data_dir` 參數，`_run_backtest_signals()` 加入 overlay 後處理（與 backtest 和 live 路徑一致），`run_consistency_check()` 自動傳遞 overlay 配置
- **G5**: 修改 `.cursor/agents/risk-manager.md` — 每週快速檢查新增「回測↔實盤一致性 replay」步驟（每個在線策略都跑 `validate_live_consistency.py`），週報新增一致性相關警戒指標
- **G6**: 明確指派：Researcher 在 GO_NEXT 前必跑一致性、Risk Manager 每週 replay
- **G7**: 修改 `.cursor/rules/strategy-dev.mdc` — Lifecycle 新增第 5 步 Overlay Ablation、Anti-Patterns 新增 overlay 相關禁令
- 修改 `docs/CURSOR_WORKFLOW.md` — 上線前 checklist 新增 Validation Matrix、overlay ablation、一致性檢查 3 項

### Telegram Bot 持倉歸類修正 + /trades UX 重寫
- 修改 `src/qtrade/monitor/multi_strategy_bot.py` — 排他式持倉歸類（共用幣種只歸 Real 策略，Paper 策略顯示佔位符）
  - 新增 `_is_paper()`, `_strategy_label()`, `_group_positions_by_strategy()`
  - Paper 策略顯示 🧪 標籤，不從 Binance API 歸類倉位
  - `/trades` 完全重寫：合併成交（by order_id）、智慧小數、中文動作標籤（開多/開空/平多/平空）、策略歸類標籤、底部匯總
  - 所有命令（`/dashboard`, `/status`, `/positions`, `/risk`, `/signals`, `/health`, daily summary）使用排他式歸類
- 修改 `scripts/run_telegram_bot.py` — 新增 `--paper-strategies` CLI 參數，自動偵測 paper 策略（config 檔名含 `paper` 或策略名含 `oi_liq_bounce`）
- 更新 `.cursor/agents/devops.md` — Telegram Bot section 新增 paper 偵測與排他式歸類說明、可用命令表新增 `/trades`

### 新增 /eod Cursor 命令
- 新增 `.cursor/commands/eod.md` — End-of-Day 文件同步檢查（6 步：git diff 掃描 → CLI_REFERENCE 判斷 → Living docs 檢查 → Lint → 更新 recent-changes → 最終摘要）
