---
alwaysApply: true
---
# Recent Changes (Rolling Log)

Agent 在每次 session 結束時更新此檔案。保留最近 7 天的條目（精簡格式），超過的刪除。
新 Chat 開啟時 agent 可從此處了解最近發生的所有變更。

---

## 2026-03-11

### Root README Refresh（Quant Developer）
- 重寫 `README.md`，移除早期 spot-era / `rsi_adx_atr` 教學內容，改成目前 futures-first 的 repo landing page，對齊現行 production candidate、研究工作流、主要 scripts、關鍵 docs、以及 anti-bias guardrails（`README.md`）
- 將根 `README.md` 進一步拆成「外部訪客版」與「內部操作者版」兩層，簡化首頁內容，並把詳細操作入口明確導向 `docs/CURSOR_WORKFLOW.md` 與 `docs/CLI_REFERENCE.md`（`README.md`）

### EOD Command Sync（Quant Developer）
- 更新 `.cursor/commands/eod.md`，讓 `/eod` 對齊現行 hygiene / orchestration 規則：補上 `tasks/` 與 root file 掃描、把 `DATA_STRATEGY_CATALOG.md` 觸發條件擴到 `prod_*.yaml`、把 orchestration 相關 doc 檢查擴到 `docs/ORCHESTRATION_MVP.md`，並把 `recent-changes` 清理窗口修正為 7 天（`.cursor/commands/eod.md`）
- 進一步把 `.cursor/commands/eod.md` 收斂成 workspace-first 版本：優先依本次 chat touched files 與當前 worktree 判斷變更範圍，只把 `git log` 當補充背景，避免把舊 commit 誤判成本次 session 工作（`.cursor/commands/eod.md`）

## 2026-03-10

### Forced Deleveraging Reversal Satellite EDA — **SHELVED**（Alpha Researcher）
- 完成 `forced_deleveraging_reversal` 研究，最佳 thesis-consistent trigger 為 `cascade_end` + `no_htf_gate`，但受限於 historical liquidation-state 不存在與早期 coverage 問題，最終判定暫不 handoff（`docs/research/20260310_forced_deleveraging_reversal_proposal.md`）

### Portfolio Strategist Agent + Portfolio Skills（Quant Developer）
- 新增 `@portfolio-strategist` 與 4 份 portfolio skills，工作流升級為 6-agent，補齊 baseline weakness diagnosis / complementary design / research routing 能力（`.cursor/agents/portfolio-strategist.md`、`.cursor/skills/portfolio/`、`docs/CURSOR_WORKFLOW.md`）

### Avg Trade Size Ablation — **FAIL**（Quant Developer）
- 實作 `avg_trade_size_overlay()` / `avg_trade_size_filter()` 並完成 6-config ablation；overlay 全害、filter 弱害、HTF replacement 近中性，結論不進生產（`src/qtrade/strategy/filters.py`、`scripts/research_avg_trade_size_ablation.py`）

### Low-Frequency Portfolio Layer Audit — **KEEP_BASELINE**（Quant Developer）
- 完成 low-frequency portfolio layer 正式審計；回撤改善主要來自大幅降曝險，非穩健 portfolio layer，維持 baseline（`config/research_low_freq_portfolio_layer.yaml`、`scripts/research_low_freq_portfolio_layer.py`）

### Signal Replay Verification（Quant Developer + DevOps）
- 新增 `verify_signal_replay.py`，Oracle Cloud 每日 cron 比對回測與實盤 SQLite signals；近 3 天 360 信號 100% match rate（`scripts/verify_signal_replay.py`、`.cursor/agents/devops.md`）

### Research Orchestrator MVP（Quant Developer）
- 新增 `@orchestrator` agent 與 `/start-research`、`/task-status`、`/approve-stage`、`/resume-task`，把研究任務改為單一入口 + task manifest + heartbeat + approval gating（`.cursor/agents/orchestrator.md`、`.cursor/commands/`）
- 新增 `docs/ORCHESTRATION_MVP.md`、`tasks/_templates/research_task_manifest.yaml`、`tasks/pilots/`，並把 `docs/CURSOR_WORKFLOW.md` 從人工 dispatcher 改寫為 approval-based supervisor 模式

### Research Orchestrator V2（Quant Developer）
- 將 research review gate 改成 `low-confidence only`，預設由 orchestrator 自動串 strategist → researcher → final packet，只在低信心 / blocker / 高風險時打斷使用者（`docs/ORCHESTRATION_MVP.md`、`docs/CURSOR_WORKFLOW.md`、`.cursor/agents/orchestrator.md`）
- task manifest 升級為 rollback-aware system of record，新增 `confidence_level`、`review_required`、`code_audit`、`rollback`、`final_packet`，並同步更新 active/pilot manifests（`tasks/_templates/research_task_manifest.yaml`、`tasks/active/`、`tasks/pilots/`）

## 2026-03-05

### Factor Hygiene Defense Framework（Quant Developer）
- 新增 factor orthogonality 工具、Regime-Stratified CPCV、研究流程門檻與 CI guard，將因子幾何審計正式納入方法學（`src/qtrade/validation/factor_orthogonality.py`、`scripts/analyze_factor_geometry.py`、`tests/test_factor_orthogonality.py`）

### Tick-level OFI EDA — **WEAK GO**（Alpha Researcher）
- 研究確認 alpha 更可能存在於 order-flow 結構而非方向；`avg_trade_size` 成為唯一值得 handoff 的微結構方向，OFI 方向特徵全面失敗（`scripts/research_tick_ofi_eda.py`、`reports/research/tick_ofi_eda/`）

## 2026-03-04

### Architecture / Governance Hardening（Quant Developer）
- 補齊 observation gate、red flags config、live vs backtest drift、Feature Ownership 與 architecture gap escalation，持續把流程規範化（`.cursor/skills/risk/periodic-review.md`、`.cursor/skills/dev/feature-ownership-registry.md`、`config/validation.yaml`）

### 6-Symbol Production Candidate Refresh（Quant Developer + DevOps）
- 移除 ADA / BNB，簡化 ETH tier，啟用 volume-based slippage，`prod_candidate_simplified.yaml` 以 6-symbol 架構上線 Oracle Cloud，進入 observation period（`config/prod_candidate_simplified.yaml`、`.cursor/agents/devops.md`）

### Alpha Decay Governance（Quant Developer）
- 新增 alpha decay governance skill，修正 `ic_monitor.py` 小分母問題並校準門檻，讓 decay 檢查成為有 owner 的正式 gate（`src/qtrade/validation/ic_monitor.py`、`.cursor/skills/validation/alpha-decay-governance.md`）
