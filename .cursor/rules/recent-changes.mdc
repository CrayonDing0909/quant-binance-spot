---
alwaysApply: true
---
# Recent Changes (Rolling Log)

Agent 在每次 session 結束時更新此檔案。保留最近 7 天的條目（精簡格式），超過的刪除。
新 Chat 開啟時 agent 可從此處了解最近發生的所有變更。

---

## 2026-03-04

### Architecture Gap Escalation（Quant Developer）
- **更新 `.cursor/skills/risk/periodic-review.md`**：新增「Architecture Gap Scan」月度檢查步驟
  - 6 個觸發條件（無主模組、校準過期、門檻異常、gate 永過/永敗、未登記模組）
  - 三級嚴重度（HIGH/MEDIUM/LOW）+ 升級路徑
  - 月報輸出格式 `ARCHITECTURE_GAP [severity]: ...`

### Feature Ownership Framework（Quant Developer）
- **新增 `.cursor/skills/dev/feature-ownership-registry.md`**：所有 critical module 的中央 ownership 登記表（16 模組），含 Governance Template
- **更新 `session-checklist.mdc`**：新增觸發條件「新增模組/gate/critical-path 功能 → 更新 registry」
- **更新 3 個 agent `.md`**（quant-researcher / quant-developer / risk-manager）：各加 Feature Ownership Registry skill 引用
  - Researcher：驗證時確認所有 gate 有方法學 owner
  - Developer：新增模組必須加入 registry 才算完成
  - Risk Manager：週期審查時檢查是否有無主模組

### Symbol Governance + Cost Model Hardening + ETH Simplification（Quant Developer）
- **移除 ADAUSDT**：結構性 IC 衰退（2024-2026 三年負 IC），overall IC +0.030 但 recent IC = -0.013
- **移除 BNBUSDT**：overall IC +0.002, p=0.68，從未有統計顯著信號
- **保留 6 symbols**：BTC, ETH, SOL, DOGE, AVAX, LINK
- **Allocation 重分配**：SOL 0.35→0.40, AVAX 0.35→0.40
- **ETH 簡化**：eth_enhanced → default tier（signal corr=1.000，w_tsmom/w_oi/w_fr/w_basis 在 confirmatory mode 為 dead params）
- **啟用 volume-based slippage model**：square-root market impact（DOGE avg 9.8bps, AVAX 10.9bps, LINK 10.4bps vs fixed 3bps）
- **驗證**：Portfolio SR 3.17（realistic cost）, Return +288%, MDD 4.40%, Calmar 6.48
  - Quick validation: 7 PASS / 1 WARN, Full validation: 10 PASS / 2 FAIL / 1 WARN
  - WFA 6/6 OOS+, CPCV PBO 2.6%, DSR 1.88(p<0.001), Embargo OOS 6/6 positive
- **部署**：Oracle Cloud `meta_blend_live` 重啟，6/6 symbols 啟動成功
- **觀察期**：2026-03-04 ~ 2026-03-18
- 更新 `devops.md`（6-symbol 架構資訊）

### Alpha Decay Governance — Agent Ownership + Code Fix（Quant Developer）
- **新增 `.cursor/skills/validation/alpha-decay-governance.md`**：Alpha Decay 方法學單一真相（ownership model、IC 公式 spec、小分母保護規則、三層 gate、校準協議、升級路徑）
- **BUG 修復 `ic_monitor.py`**：新增 `min_ic_denominator` 參數（default 0.01），當 `|historical_ic| < min_ic_denominator` 時改用絕對差取代比率，防止 +240%/+1500% 等荒謬衰退值
- **門檻校準 `validation.yaml`**：`recent_ic_min` 0.02→0.005（TSMOM 結構性低 IC）、`max_decay_pct` 0.5→0.6、`max_critical_alerts` 0→2、新增 `min_ic_denominator: 0.01`
- **`validate.py`** 同步更新：ValidationConfig defaults + `run_alpha_decay_check()` 新增 `min_ic_denominator` 穿透
- **Agent 檔案更新**：
  - `quant-researcher.md`：新增「Alpha Decay 方法學 (OWNER)」職責 + governance skill 引用
  - `quant-developer.md`：自我檢查清單 +12「門檻改動需有 Researcher 校準依據」+ skill 引用
  - `risk-manager.md`：新增「Alpha Decay Action Protocol」（Mild→WARNING / Moderate→REDUCE / Severe→FLATTEN）+ skill 引用
- **Skill 更新**：
  - `validation/pipeline.md` Step 10：替換模糊描述為具體三層 gate 判定 + FAIL 升級路徑
  - `risk/periodic-review.md`：新增 alpha decay FAIL 操作協議（含嚴重程度分級）
  - `dev/toolchain-integrity-checklist.md`：新增 §5 IC 監控 Pre-Flight（小分母保護、門檻校準、常見陷阱）
- **測試**：`test_ic_monitor.py` +2 tests（小分母 guard + 參數接受），`test_validation_pipeline.py` +1 test（defaults 校驗）→ 22 passed

## 2026-03-03

### P0 技術債務清理（DevOps）
- 歸檔 2 個已完成 research scripts → `scripts/archive/`（research_orderflow_composite_eda.py, research_vpin_ablation.py）
- 歸檔 4 個已完成 research configs → `config/archive/`（research_orderflow_composite.yaml, research_vpin_ablation_{A,B,C}.yaml）
- 清理後：0 個 research_* 在活躍目錄，41 active scripts, 18 active configs
- 重新生成 `CLI_REFERENCE.md`（210 行）+ `DATA_STRATEGY_CATALOG.md`（110 行）
- `validation.yaml` trial_registry 已確認包含全部 22 個研究方向（#18 VPIN, #19 Entropy, #20 Orderflow Composite），n_trials=2463 已最新
- 修正 3 處 ghost references：`STRATEGY_DEV_PLAYBOOK_R2_1.md`（research_overlay_4way.py）、`STRATEGY_PORTFOLIO_GOVERNANCE.md`（research_strategy_blend.py）、`meta-blend-pattern.md`（同上 + config 路徑更新為 prod_candidate_simplified.yaml）

### Validation Gap Hardening（Quant Developer）
- `scripts/validate.py` 新增 `run_alpha_decay_check()`，將 alpha decay 納入正式 validation gate（含 `--only alpha_decay` 與 summary PASS/FAIL）
- `config/validation.yaml` 新增 `alpha_decay` section（forward_bars/window_days/recent_days/recent_ic_min/max_decay_pct/max_critical_alerts）
- 新增 `tests/test_ic_monitor.py`（3 tests）+ 擴充 `tests/test_validation_pipeline.py`（2 tests）覆蓋 alpha_decay config 與 summary 輸出
- 更新 `docs/STRATEGY_DEV_PLAYBOOK_R2_1.md` Validation Matrix：新增 V10 Alpha Decay gate 並調整編號
- 測試：`pytest tests/test_ic_monitor.py tests/test_validation_pipeline.py -q` → 19 passed

## 2026-03-02

### Alpha Decay Cron 自動化（DevOps）
- 新增 `scripts/cron_alpha_decay.sh`：每週日 00:00 UTC 自動執行 alpha decay 掃描，報告輸出到 `reports/alpha_decay/`，有警報時 Telegram 通知
- 更新 `.cursor/agents/devops.md`：新增 Alpha Decay Cron 設定說明 + crontab 範例
- 更新 `.cursor/skills/risk/periodic-review.md`：Weekly Quick Check Alpha Decay 改為讀取 cron 報告（不再手動執行），同步更新 config 引用為 `prod_candidate_simplified.yaml`
- 重新生成 `CLI_REFERENCE.md`（+cron_alpha_decay.sh）+ `DATA_STRATEGY_CATALOG.md`

### Data Embargo Infrastructure — OOS 數據隔離機制（Quant Developer）
- **新增 `data_embargo` 到 `config/validation.yaml`**：時間 embargo（最近 3 個月鎖死不用於研究）+ 幣種 embargo（AVAXUSDT/DOTUSDT 保留做 cross-symbol OOS）
- **新增 `src/qtrade/validation/embargo.py`**：完整 embargo 工具模組
  - `EmbargoConfig` / `TemporalEmbargoConfig` / `SymbolEmbargoConfig` dataclasses
  - `load_embargo_config()` — 從 validation.yaml 載入
  - `enforce_temporal_embargo(df)` — 強制截斷 DataFrame 到 cutoff 之前
  - `get_research_symbols()` / `is_symbol_embargoed()` / `get_embargo_only_symbols()` — 幣種篩選
  - `run_embargo_holdout_test()` — 用 embargo 區間做 holdout IS vs OOS 比較
  - `print_embargo_status()` — 印出當前 embargo 狀態
- **整合到 `scripts/validate.py`**：新增 step 8.5 Embargo Holdout OOS Test
  - ValidationConfig 新增 `embargo_holdout_enabled` / `embargo_holdout_max_degradation`
  - Quick mode 自動跳過
  - `generate_summary()` 顯示 embargo holdout 結果
- **新增 CI guard `tests/test_embargo_guard.py`**（9 tests）
  - 自動掃描 `scripts/research_*.py` 確保使用 embargo 機制
  - 完整 API 測試、config 載入測試、截斷/過濾功能測試
- **更新 2 個活躍 research scripts** 加入 `EMBARGO_EXEMPT` 標記（已完成研究）
- **更新 `src/qtrade/validation/__init__.py`**（+12 exports from embargo）
- **更新 `.cursor/rules/research-methodology.mdc`**（新增 §8 Data Embargo 規則）
- 重新生成 `CLI_REFERENCE.md` + `DATA_STRATEGY_CATALOG.md`
- 452 測試全通過（+9 新增）

### VPIN Regime EDA — **WEAK GO**（Alpha Researcher / Quant Developer）
- **aggTrades Data Pipeline**: 新增 `src/qtrade/data/agg_trades.py`（667行）+ `binance_vision.py` aggTrades 支援
  - Volume-clock bars (Easley 2012), VPIN, Real CVD, OFI 計算
  - 新增 `scripts/download_aggtrades_data.py`：8 symbols 2020-2026 下載+處理（~50K hourly bars/symbol）
  - Bug fix: Binance Vision 2024+ aggTrades CSV header 自動偵測, `io.BytesIO` wrapper for reliable seek
- **EDA 結果** (notebook: `notebooks/research/20260301_vpin_regime_eda.ipynb`):
  - **Confounding OUTSTANDING**: corr(VPIN, ATR_pctrank) = **0.025** — 歷來最佳, 遠低於 0.6 kill threshold
  - IC = 0.005 (< 0.01, A5 FAIL), 但 7/8 same sign (A3 PASS), vpin_delta_6 6/7yr (A1 PASS)
  - Quintile: Q1(low VPIN) SR = **1.845** vs Q5(high VPIN) SR = **0.870**
  - Filter P70: ΔSR +0.117, freq loss 30%, 6/8 improved
  - Low VPIN regime SR = 1.136 vs High VPIN SR = 0.489, 6/8 symbols Low > High
  - VPIN IC < ATR IC < RV IC, 但 VPIN 與 vol 完全正交(corr=0.025)
  - **Quality Gates 5/6 PASS → WEAK GO**
  - **關鍵洞察**: 之前 OI/On-chain/Macro 疊加 HTF 都 over-filter（因為與 vol 部分相關），VPIN 完全正交可能避免此問題
- 更新 `ALPHA_RESEARCH_MAP.md` (#18 WEAK GO, 數據-信號圖譜更新, 研究前沿排序, Top 3 更新)
- 更新 `data/__init__.py` (+lazy import agg_trades)
- 重新生成 `CLI_REFERENCE.md` + `DATA_STRATEGY_CATALOG.md`
- **Handoff**: @quant-developer ablation A(HTF only)/B(VPIN only)/C(HTF+VPIN)
- Data: `data/binance/futures/aggtrades_agg/` (8 symbols × 5 files each)

### VPIN Regime Filter Ablation — **KEEP_BASELINE**（Quant Developer）
- 實作 `vpin_regime_filter()` 到 `src/qtrade/strategy/filters.py`（gate pattern, pctrank >= 0.70 block）
- 整合到 `tsmom_carry_v2_strategy.py`（HTF filter 之後，opt-in disabled by default）
- 新增 `_load_and_align_vpin()` helper（VPIN 載入 + causal forward-fill align）
- 3-config ablation: A(HTF only)=SR 3.846, B(VPIN only)=SR 3.980(+3.5%), C(HTF+VPIN)=SR 3.798(-1.2%)
- **B standalone**: 7/8 symbols improved (only BNB -0.026), Return +500% vs A +391%, Calmar 7.87 vs 6.89
- **C stacking**: 4/8 improved, -1.2% — 正交性減輕退化(vs macro -9.8%) 但仍無法完全消除
- **關鍵發現**: 所有 4 個 filter (OI/On-chain/Macro/VPIN) standalone 均優於 HTF 但 stacking 均退化 → gate math 問題（多重 gate 降 TIM）非 signal redundancy
- **Verdict: KEEP_BASELINE** — B +3.5% < 5% threshold, C -1.2%
- Code preserved, configs archived to `config/archive/`
- 更新 `docs/ALPHA_RESEARCH_MAP.md`（#18/#14 KEEP_BASELINE, 完成 ablation section, Top 3 → filter replacement 框架, 覆蓋摘要更新）
- Report: `reports/research/vpin_ablation/ablation_results.json`
- Script: `scripts/research_vpin_ablation.py`（保留供審計）

### Entropy Regime Indicator EDA — **FAIL**（Alpha Researcher）
- 3 entropy measures (Permutation Entropy, Shannon Entropy, Approximate Entropy) × 3 lookbacks (24/168/720h) = 9 indicators, 8 symbols, 2020-2026
- **Confounding 全通過**: 7/7 entropy indicators survive (|corr(vol)| < 0.2) — 與 VP (corr=0.71) 完全不同
- **但所有 IC < 0.01 (A5 FAIL)**: 最強 pe_720 avg IC = -0.0024, 7/8 same sign
- Year-by-year: 5/7 consistent (A1 PASS), shift impact -4.3% (A2 PASS)
- Quintile spread Q1-Q5 = +0.21 annualized Sharpe (方向正確但極弱, 0/8 monotonic)
- Binary filter (block P80): 僅 3/8 improved
- **結論**: Entropy ≠ vol proxy（獨立維度）但 ≠ alpha source。加密 1h 序列太接近 random walk
- 更新 `ALPHA_RESEARCH_MAP.md` (新增 #20 FAIL, Dead Ends, 數據-信號圖譜, 更新紀錄)
- 更新 `RESEARCH_LITERATURE.md` (新增 5 篇 entropy 文獻)
- 更新 failure-registry.md (新增 entropy 失敗教訓)
- Script: `scripts/archive/research_entropy_regime_eda.py`
- Report: `reports/research/entropy_regime_eda/entropy_regime_eda_results.json`

### P1 Validation Pipeline Hardening（Quant Developer）
- **Cost Stress Test**: `config/validation.yaml` 新增 `cost_stress` section（multipliers: [1.5, 2.0]），`scripts/validate.py` 新增 `run_cost_stress_test()`
  - Per-symbol table: SR at 1×/1.5×/2× cost，PASS = 2× cost 下 SR > 0
  - 使用 `copy.deepcopy(cfg)` 修改 `fee_bps` + `slippage_bps`，避免 cfg mutation
- **Delay Stress Test**: `config/validation.yaml` 新增 `delay_stress` section（+1 bar, max_drop 50%），`scripts/validate.py` 新增 `run_delay_stress_test()`
  - 在正常 delay=1 上額外 shift(1) pos，重建 portfolio，比較 SR drop %
  - PASS = avg SR drop < 50%
- **Holdout OOS Test**: `config/validation.yaml` 將 `oos_testing` 改名 `holdout_oos`（移除「供參考」），新增 `enabled: true` + `max_degradation: 0.5`
  - `scripts/validate.py` 新增 `run_holdout_oos()`：per-period IS/OOS SR 比較
  - PASS = OOS SR > IS SR × (1 - max_degradation) 且 avg OOS SR > 0
- **統一 Summary 改進**: `generate_summary()` 新增 5 個測試結果顯示
  - 原有: WFA, MC, Cross-Asset, DSR, PBO, Kelly
  - 新增: Cost Stress, Delay Stress, Holdout OOS, Regime Split, Red Flags
  - 總計 11 項驗證，每項顯示 PASS/FAIL/WARN + 一句話解釋
  - Quick mode 自動跳過 cost/delay/holdout（節省時間）
- 重新生成 `CLI_REFERENCE.md` + `DATA_STRATEGY_CATALOG.md`
- 443 測試全通過

### P0 統計嚴謹性審計修復（Quant Developer）
- **DSR n_trials 追蹤**: `config/validation.yaml` 新增 `trial_registry` section（cumulative_n_trials=2000），`scripts/validate.py` 改從 trial_registry 讀取
  - 根據 ALPHA_RESEARCH_MAP.md 估算 21 方向 × ~95 變體/方向 ≈ 2000
  - `failure-registry.md` 新增維護規則：每次研究方向結束後必須更新 n_trials
- **Regime-Specific Performance Report**: 新增 `src/qtrade/validation/regime_analysis.py`
  - `compute_regime_performance()` — 對每個 regime 計算 SR/Return/MDD/Win Rate/Avg Trade Duration
  - `auto_detect_regimes()` — 基於 BTC drawdown >20% = bear, >50% rally = bull, else sideways
  - 整合到 `scripts/validate.py` pipeline，新增紅旗規則 SR<0 → WARNING
- **Backtest Red Flag 機制**: 新增 `src/qtrade/validation/red_flags.py`
  - `check_red_flags()` — 4 條規則: Sharpe>4.0, MDD<3%, WinRate>70%, PF>5.0
  - 整合到 `scripts/run_backtest.py` + `scripts/validate.py`
  - 新增 CI test `tests/test_red_flag_integration.py`（7 tests）
- 更新 `src/qtrade/validation/__init__.py`（exports: RegimePerformance, compute_regime_performance, auto_detect_regimes, check_red_flags）
- 重新生成 `CLI_REFERENCE.md` + `DATA_STRATEGY_CATALOG.md`
- 443 測試全通過（+30 新增）

### Orderflow Composite Standalone Strategy — **FAIL**（Alpha Researcher）
- **首次獨立策略嘗試**（非 filter/overlay）：OFI+VPIN proxy+CVD 3-signal composite from taker_vol_ratio
- 新增 `src/qtrade/strategy/orderflow_composite_strategy.py`：完整獨立策略（contrarian/momentum 雙模式、threshold positioning）
- 新增 `scripts/research_orderflow_composite_eda.py`：3-phase EDA+Backtest+Portfolio Blend
- 新增 `config/research_orderflow_composite.yaml`：研究配置
- **EDA 結果** (8 symbols, 2022-2026):
  - **corr(TSMOM) = -0.023** — 歷來最低！微結構 ⊥ TSMOM 完全正交
  - IC(24h) = +0.003 (composite momentum, 6/8 same sign) — 太弱 (A5 FAIL)
  - Individual: ofi_signal_72 IC=+0.011(6/8), vpin_proxy IC=-0.028(8/8), cvd_trend IC=-0.014(7/8)
  - Contrarian mode best: 8/8 symbols positive SR (pre-cost)
  - Pre-cost SR = 0.378, MDD = -55.5%
- **Backtest**: Post-cost SR 深度負值。Threshold positioning 降 turnover 63% 仍不夠（年化成本 ~143% >> alpha ~5.7%）
- **Portfolio Blend**: corr 極低但 orderflow SR 太弱，blend 無改善
- **根因**: taker_vol_ratio 1h proxy 解析度不足，大量 tick-level 信息在小時聚合時丟失
- 更新 `ALPHA_RESEARCH_MAP.md`（新增 #21 FAIL, Dead Ends, 數據-信號圖譜, 研究前沿更新, Top 3 更新: tick-level OFI 替代 proxy）
- Strategy code preserved: `orderflow_composite_strategy.py`
- Script+Config archived: `scripts/archive/`, `config/archive/`
- Report: `reports/research/orderflow_composite/orderflow_eda_results.json`

## 2026-03-01

### 技術債務清理：Research Scripts/Configs 歸檔（Quant Developer）
- 移除 6 個重複檔案（已在 archive/ 但未從活躍目錄刪除）
  - `scripts/`: research_ic_deep_analysis.py, research_overlay_ic_analysis.py
  - `config/`: research_oi_ablation_{A,B,C}.yaml, research_xsmom.yaml
- 歸檔 6 個已完成 research scripts → `scripts/archive/`
  - research_btc_htf_variants.py (tier ablation DONE)
  - research_macro_regime_{eda,ablation}.py (FAIL)
  - research_overlay_4way.py, research_strategy_blend.py (DONE→prod)
  - research_vp_vs_vol_ablation.py (FAIL)
- 歸檔 16 個已完成 research configs → `config/archive/`
  - HTF/LSR/meta_blend 系列 (DONE→prod simplified)
  - On-chain ablation A/B/C (KEEP_BASELINE)
  - OI liq bounce (SHELVED), param refresh, risk mitigation, extended spot
- 清理後：0 個 research_* 在活躍目錄，37 active scripts, 18 active configs
- 重新生成 `CLI_REFERENCE.md`（242→209 行）+ `DATA_STRATEGY_CATALOG.md`
- 413 測試全通過

### Binance Vision aggTrades CSV header 修復（Quant Developer）
- **BUG 修復**: `binance_vision.py` `download_single_month_aggtrades()` — Binance Vision 2024+ 的 aggTrades CSV 新增 header 行
  - 原 `header=None` 導致 `invalid literal for int() with base 10: 'agg_trade_id'` 錯誤
  - 修復：自動偵測 CSV 是否有 header（peek 首行），兩種格式都相容
- 413 測試全通過
- 重新生成 `CLI_REFERENCE.md` + `DATA_STRATEGY_CATALOG.md`

### aggTrades Data Pipeline + VPIN/OFI Research Infrastructure（Quant Developer）
- 新增 `binance_vision.py`: `generate_monthly_aggtrades_urls()` + `download_single_month_aggtrades()` — 支援 Futures USDT-M aggTrades 下載
- 新增 `src/qtrade/data/agg_trades.py` 模組：
  - `aggregate_trades_to_hourly()` — 逐筆成交聚合為小時級 buy/sell volume
  - `compute_volume_clock_bars()` — Easley (2012) volume-clock bars
  - `compute_vpin_from_bars()` — VPIN (Volume-Synchronized Probability of Informed Trading)
  - `compute_real_cvd()` — 真實 CVD（替代 taker_vol_ratio proxy）
  - `compute_ofi_hourly()` — 簡化版 OFI (Cont 2014)
  - `download_and_process_aggtrades()` — 逐月下載 + 即時聚合 + 自動儲存
  - Load/align helpers: `load_vpin()`, `load_real_cvd()`, `load_ofi()`, `align_vpin_to_klines()`
- 新增 `scripts/download_aggtrades_data.py`：8 symbols 2020-2026 下載腳本（含 --coverage 報告）
- 新增 `notebooks/research/20260301_vpin_regime_eda.ipynb`：完整 7-section EDA
  - 含 confounding check (corr(VPIN, ATR_pctrank) kill criterion)
  - Quality gates A1-A5, G1-G6 全自動評估
  - VPIN quintile analysis + filter effect + direction interaction
  - 設計先查 confounding，避免重蹈 VP va_width_pct 覆轍
- 更新 `data/__init__.py`（+`_lazy_import_agg_trades`）
- 重新生成 `CLI_REFERENCE.md` + `DATA_STRATEGY_CATALOG.md`
- 413 測試全通過

### VP va_width_pct vs Simple Vol Ablation — **FAIL**（Quant Developer）
- 3-way ablation: A(baseline)=SR 3.62, B(VP gate)=SR 3.23(-10.9%), C(vol gate)=SR 2.92(-19.5%)
- VP > Vol 在 7/8 symbols（VP 確含些微超越 vol 的信息）
- 但兩者都劣於 baseline → **VP 無生產增量**
- corr(VP, realized_vol) = 0.71（高混淆）
- 結論：va_width_pct 本質是 vol regime proxy，既有 HTF filter 已充分處理 vol regime
- 更新 `docs/ALPHA_RESEARCH_MAP.md`（#17 FAIL, #13 FAIL, Dead Ends 新增, 覆蓋地圖更新）
- Ablation report: `reports/research/vp_vs_vol_ablation/ablation_results.json`
- Research script: `scripts/research_vp_vs_vol_ablation.py`（保留供審計）

### Macro Cross-Market Regime EDA — **GO**（Alpha Researcher）
- 26 macro indicators (VIX/SPY/QQQ/DXY/US10Y/GLD × momentum/pctrank), 8 crypto symbols, 2019-2026
- **Top signals**: GLD_mom_60d IC=-0.049 (8/8 same sign, 6/8yr), VIX_mom_30d IC=+0.043 (8/8yr consistent!)
- **Combined (GLD+VIX)**: IC=0.056 (>individual), GLD⊥VIX corr=0.053
- **獨立性 OUTSTANDING**: corr(BTC_mom)=0.125, corr(TVL)=0.034 → 最獨立有效信號
- 5/5 quality gates PASS for all top indicators
- Binary filter (block bottom 20%): **8/8 improved** Δ SR +0.20~0.61, Risk-On SR=1.04 vs Risk-Off SR=-1.24
- 經濟意義：gold rally = flight-to-safety = risk-off → crypto weak; VIX spike → contrarian recovery
- 5-factor score **4.3**（歷史最高），→ **GO, Handoff Quant Dev mandatory ablation A/B/C**
- 更新 `docs/ALPHA_RESEARCH_MAP.md`（#19 新增, #15 GO, 圖譜更新, 覆蓋地圖更新）
- EDA script: `scripts/research_macro_regime_eda.py`
- Report: `reports/research/macro_regime_eda/macro_regime_eda_results.json`

### Macro Cross-Market Regime Ablation — **KEEP_BASELINE**（Quant Developer）
- 實作 `macro_regime_filter()` 到 `src/qtrade/strategy/filters.py`（gate pattern, pctrank < 0.2 block）
- 整合到 `tsmom_carry_v2_strategy.py`（onchain filter 之後，opt-in disabled by default）
- 新增 `_load_and_align_macro_indicator()` helper（GLD+VIX download, momentum, combined score, shift(1)）
- 3-config ablation: A(HTF baseline)=SR 2.413, B(Macro only)=SR 2.525(+4.6%), C(HTF+Macro)=SR 2.177(-9.8%)
- **B standalone**: 7/8 symbols improved (BNB slightly worse), +4.6% but < 5% threshold
- **C stacking**: 0/8 symbols improved, severe over-filter (-9.8%)
- **Pattern**: 同 OI (+4.66%) 和 On-chain (+5.3%) — standalone > HTF baseline 但疊加退化
- **Verdict: KEEP_BASELINE** — code preserved, 觀察期後(3/14)可評估 filter replacement
- 更新 `docs/ALPHA_RESEARCH_MAP.md`（#19 KEEP_BASELINE, #15 KEEP_BASELINE, Dead Ends, 覆蓋地圖更新）
- Ablation script: `scripts/research_macro_regime_ablation.py`
- Report: `reports/research/macro_regime_ablation/ablation_results.json`

## 2026-02-28

### Post-Fix Impact Verification（Quant Developer）
- 重跑生產 portfolio backtest：SR=3.80, Return=+387.06%, MDD=-4.99%, Calmar=6.80 — **與修復前完全一致**
- Per-symbol: 8/8 positive SR (2.06~3.44)，無任何變化
- Live: 修復尚未 commit/deploy，Oracle Cloud 實盤不受影響
- WFA 重跑：8/8 OOS+, avg degradation 改善 28.4% → -1.3%
  - 原因：overlay_pipeline 統一 + params copy 修復了 WFA split 間的 overlay 交叉汙染
  - 策略本身更穩健（負 degradation = OOS 優於 IS），舊 WFA 數字是被汙染的
- 更新 `prod_candidate_simplified.yaml` WFA 行註記

### Agent 防錯機制強化 — 規則 + CI 自動掃描（Quant Developer）
- 新增 `.cursor/rules/code-safety.mdc`（alwaysApply: true）：5 類反模式 BAD/GOOD 範例
  - 淺複製嵌套 dict、caller params mutation、signal_delay 遺漏、defaults 分歧、critical path silent except
- 新增 `tests/test_code_safety_guard.py`（6 tests）：靜態掃描 5 類違規
  - 掃描 config dict .copy()、params.pop()、StrategyContext 缺 signal_delay、silent except、直接 vbt.Portfolio.from_orders
- 更新 `quant-developer.md` 自我檢查清單（+3 項：deepcopy、params.get、dataclass defaults）
- 更新 `toolchain-integrity-checklist.md`（+Section 4 Config Mutation Prevention）
- 全部 413 測試通過

### Extended Spot Backtest — 8.5 年 TSMOM 因子驗證（Quant Developer）
- 下載 Binance Spot 1h OHLCV: 6 symbols (BTC/ETH 2017-08, BNB 2017-11, ADA 2018-04, LINK 2019-01, DOGE 2019-07)
- 新增 `config/research_extended_spot.yaml`（spot, long-only, no overlay, no FR）
- Portfolio backtest (2019-07→2026-02, 6.6yr common): SR=1.52, CAGR=8.07%, MDD=4.83%
- Per-symbol: BTC SR=1.47 (8.5yr), ETH SR=1.22 (8.5yr), 6/6 positive
- Validation 全通過: WFA avg deg 35.7%, CPCV PBO avg 2.6% (max 4.4%), DSR p<0.001, LOAO robust
- **結論**: TSMOM 核心因子跨 8.5 年市場週期驗證有效，非過擬合

### 工具鏈健康審計 + 修復（Quant Developer）
- **BUG 修復**: `validate.py` WFA/MC/Kelly/LOAO 全部加入 `data_dir` 穿透（之前缺失導致 funding rate 未計算）
- **BUG 修復**: `cross_asset.py` `BacktestFunction` protocol + `BaseValidator` + 所有便捷函數加入 `data_dir`
- **BUG 修復**: `kelly_validation.py` `kelly_backtest_comparison()` 加入 `data_dir`
- **棄用**: `probability_of_backtest_overfitting()` 改名 `_simplified_pbo_estimate()` 並從 `__init__.py` exports 移除
- **標準化**: `AppConfig.resolve_kline_path()` / `resolve_kline_paths()` 共用函數，統一數據路徑
- **修復**: `run_walk_forward.py` / `run_oi_bb_rv_research.py` 移除 `"klines"` 回退邏輯，改用 `resolve_kline_path()`
- **測試**: 新增 `test_validation_pipeline.py`（12 tests）+ `test_data_dir_propagation.py`（8 tests）
- **Skill**: 新增 `.cursor/skills/dev/toolchain-integrity-checklist.md`
- 全部 407 測試通過

### Agent 架構瘦身（全域）
- 5 個 agent `.md` 全面重構：抽取 18 個 skill 檔到 `.cursor/skills/{alpha,dev,validation,risk,ops}/`
- Agent 核心 `.md` 精簡至 ~70-115 行（原 185-750 行），只保留身份+核心規則+skills 索引
- `.cursorrules` 獨有內容（Binance API gotchas）移入 `skills/dev/binance-api-gotchas.md`，`.cursorrules` 刪除
- `recent-changes.mdc` 52KB→~4KB，`project-overview.mdc` 精簡至當前狀態

### Tier Ablation 研究 + Config E 簡化候選（Alpha Researcher）
- 5-config ablation: A=prod(SR 3.77), B=all default(3.57), C=all tsmom_heavy(3.57), D=BTC no-breakout(3.66), E=simplified(3.85)
- 發現: B=C → w_tsmom/w_basis_carry 是 dead params（confirmatory 模式不讀取）
- 發現: BTC breakout_vol_atr 是負貢獻（BTC SR 2.02→1.99, Δ=-0.03）, 20 params 白搭
- 發現: BTC 720h lookback 價值 +0.38 per-sym SR（1.64→2.02），僅 3 extra params
- Config E（簡化候選）: SR 3.85 > prod 3.77, MDD -3.67% < -3.91%, params -47%
- 建議: Config E → Quant Dev 正式 validation → 替換生產
- 歸檔 3 ablation configs → `config/archive/`
- 更新 `docs/ALPHA_RESEARCH_MAP.md`（Dead Ends + coverage map + 更新紀錄）

### Config E 正式 Validation + Config Freeze（Quant Developer）
- WFA: 8/8 OOS+, avg degradation 27.9%, 每個 split 都正收益
- CPCV: PBO max 0.13 (SOL/DOGE/ADA), rest 0.07 — 與 prod 完全相同
  - 4/8 symbols 有負 degradation（OOS > IS，強 anti-overfitting 信號）
- DSR: 1.42 (p<0.001) → PASS
- Cross-Asset LOAO: robust, -0.9% avg degradation
- Monte Carlo: avg VaR95% = 0.19%
- Pre-Deploy: 11 PASS / 3 WARN / 0 FAIL
- Config freeze: `prod_candidate_simplified.yaml`（SR 3.85, MDD -3.67%, params -47%）

### Risk Manager Pre-Launch Audit — APPROVED（Risk Manager）
- MC 獨立重跑 10,000 sims: 8/8 symbols VaR95% 0.18-0.20%, avg 0.19% → PASS
- Kelly: 8/8 PASS, all recommend 25% Kelly (Full Kelly 8.3-15.8%)
- Portfolio Risk: HHI=0.127 < 0.2, BTC+ETH=30% < 40%, VaR95% 0.19% << 5%
- Risk Limits: MDD -3.67% vs circuit breaker 40% → 36.3% headroom
- meta_blend: auto_delay=False ✅, no double-delay, concentration OK
- 所有變更為簡化操作（移除負 alpha + dead params），風險降低
- **Verdict: APPROVED**，14-day observation period → 2026-03-14

### Simplified 策略部署到 Oracle Cloud（DevOps）
- Config `prod_candidate_simplified.yaml` 替換 `prod_candidate_htf_lsr.yaml`
- 重建 `meta_blend_live` tmux session
- Post-deploy 驗證: 8/8 symbols ✅, tier routing 正確, overlay ON, 無 breakout
- Observation period: 2026-02-28 ~ 2026-03-14
- Rollback L1: `prod_candidate_htf_lsr.yaml` (SR=3.77)

### 第四輪架構安全修復（Quant Developer）
- `walk_forward.py` `grid_search_params()` `.copy()` → `copy.deepcopy()`（防 overlay/funding_rate 等嵌套 dict 跨迭代汙染）
- `load_config()` `PositionSizingConfig` 移除硬編碼默認值，改由 dataclass field defaults 填充（修正 vol_lookback=20 繞過新默認值 168 的問題）
- 407 測試全通過

### 第三輪架構安全修復（Quant Developer）
- `_apply_position_sizing` silent except → `logger.error()` + TG 一次性告警（防過度曝險無可見性）
- `_get_equity` log 升級 debug→warning（影響 position sizing 的失敗必須可見）
- `ConsistencyValidator._run_backtest_signals` 加 `signal_delay=1`（與 run_symbol_backtest 一致）
- `ConsistencyValidator._compare_signals` 明確 `signal_delay=0`（模擬 live）
- `PositionSizingConfig.vol_lookback` 默認值 20→168（與所有 production configs 及 backtest 一致）
- `run_backtest.py` position sizing 默認值改從 `PositionSizingConfig` 讀取（消除 backtest/live 分歧）
- 407 測試全通過

### 系統性 Look-Ahead 根因修復 + CI 防護（Quant Developer）
- 新增 `causal_resample_align()` 共用函數（`src/qtrade/strategy/filters.py`）
- 遷移 6 個非生產路徑的 look-ahead 位置
- 修正 4 份 `_resample_ohlcv()` docstring
- 新增 CI 防護 `tests/test_resample_shift_guard.py`（3 tests）
- 更新 anti-bias.mdc §5「HTF Resample + Reindex: 必須 shift(1)」
- 366 測試通過

### OI Regime Filter EDA — WEAK GO（Alpha Researcher）
- 13 OI regime indicators × 8 symbols，2022-2026，coverage 99.9%
- 所有 IC 負值（higher OI → lower returns），最強 IC=-0.006 < 0.01 (A5 WARN)
- Quintile spread: Q5-Q1 = -1.31 Sharpe（強條件效應）
- F5 filter (pctrank_720 > 0.3): Δ SR +0.317, 8/8 symbols improved, freq loss 29.8%
- 方向交互發現: Long+FallingOI SR=1.50(BEST) vs Short+FallingOI SR=0.01(DEAD)
- Quality Gates: 4 PASS + 2 WARN (G1 IC弱, G3 confounding partial)
- **Verdict: WEAK GO → Filter**, handoff @quant-developer with ablation requirement
- 更新 `docs/ALPHA_RESEARCH_MAP.md`（覆蓋地圖 +#16, 圖譜更新, 前沿排序 #2 → WEAK GO）

### On-Chain Regime Overlay EDA — **GO**（Alpha Researcher）
- 數據: DeFi Llama TVL + 穩定幣市值（12 on-chain series, 2017-2026, 免費）
- 18 regime indicators × 8 symbols，coverage ≥70% (13/16 metrics pass)
- **tvl_mom_30d IC=0.065**（>10× OI 的 IC=0.006），A1-A5 全 PASS
  - A1: 5+/2- years ✅, A2: shift impact 5.2% ✅, A3: 8/8 same sign ✅, A5: IC≥0.01 ✅
- **tvl_sc_ratio_mom_30d quintile spread +4.69 (monotonic!)**
- Filter ≥P30: **8/8 symbols improved, avg Δ SR = +0.409**, freq loss ~30%
- Risk-On vs Risk-Off: 8/8 symbols Risk-On better, avg Δ SR = +1.454
- G6: avg |corr| = 0.302 with BTC price momentum（部分獨立信息）
- **Quality Gates: 6/6 PASS → GO**
- 修正 stablecoin 下載腳本 timestamp parsing bug（`int(ts)` cast）
- Handoff: @quant-developer 實作 `onchain_regime_filter()` in `filters.py`
  - 必須 ablation: (A) HTF only, (B) On-chain only, (C) HTF + On-chain
- Notebook: `notebooks/research/20260228_onchain_regime_overlay_eda.ipynb`

### On-Chain Regime Filter — 實作 + Ablation + Validation（Quant Developer + Quant Researcher）
- **BUG 修復**: `onchain_regime_filter()` lookback 365→720（1h bars，365=15天而非30天）
- 同步修正 `tsmom_carry_v2_strategy.py` fallback + 2 ablation configs
- **BUG 修復**: `prado_methods.py` CPCV 呼叫 `probability_of_backtest_overfitting` → `_simplified_pbo_estimate`
- 3-config ablation: A(HTF only)=SR 3.80, B(On-chain only)=SR 4.00, C(HTF+On-chain)=SR 3.88
- B standalone +5.3% SR, MDD -3.49%（best），但 C 疊加退化（同 OI 模式）
- Full validation on Config B: WFA 8/8 PASS (avg deg -3.2%), CPCV PBO max 0.13, DSR 2.28 p<0.001
- **Verdict: KEEP_BASELINE** — 增量 borderline(+5.3%), 觀察期內不替換, 2/8 symbols 退化
- Code preserved, configs 保留供 3/14 後重評估

### OI Regime Filter Ablation — FAIL（Quant Developer）
- 新增 `oi_regime_filter()` 到 `src/qtrade/strategy/filters.py`（gate pattern, pctrank_720 < 0.3 block）
- 整合到 `tsmom_carry_v2_strategy.py`（HTF filter 之後、turnover control 之前）
- 3-config ablation: A(HTF only)=SR 3.86, B(OI only)=SR 4.12, C(HTF+OI)=SR 4.04
- **Incremental SR = +4.66% < 5% threshold → FAIL**
- Key findings: OI standalone (+6.74%) 實際上比 HTF 更強，但疊加造成 over-filter (5/8 symbols SR 下降)
- C 的 MDD 最佳 (-3.14%) 但 return 最低 (+215%)
- Code preserved, 3 ablation configs archived to `config/archive/`
- 更新 `docs/ALPHA_RESEARCH_MAP.md`（#16 FAIL, 覆蓋地圖, 研究日誌）
- Notebook: `notebooks/research/20260228_oi_regime_filter_eda.ipynb`

### Order Flow / AMT / Volume Profile 文獻調研（Alpha Researcher）
- `RESEARCH_LITERATURE.md` Microstructure 區塊大幅擴充：
  - AMT 經典書籍 4 本（Steidlmayer 1986, Dalton 1993/2007, Steidlmayer & Hawkins 2003）
  - OFI/VPIN 學術論文 6 篇（Cont et al. 2014 ⭐, Easley et al. 2012 ⭐, Glosten & Milgrom 1985, Easley & O'Hara 1992, Bouchaud et al. 2009, Cont 2001）
  - 加密專屬 3 篇（Alexander & Heck 2020, Dyhrberg et al. 2018, Makarov & Schoar 2020）
  - 教科書 2 本（Cartea et al. 2015, O'Hara 1995）+ 實務書 2 本（Trader Dale 2018, 開源證券系列）
- `ALPHA_RESEARCH_MAP.md` 新增：
  - 覆蓋地圖 #17 (Volume Profile POC/VA filter) + #18 (VPIN regime filter)
  - 數據-信號圖譜 3 條目 (VP/VPIN/OFI from aggTrades)
  - 研究前沿 #13 (VP, **3.6分** — 最高候選) + #14 (VPIN, 3.3分)
  - Top 3 建議更新：VP → VPIN → retail_vs_top LSR

### Volume Profile EDA — WEAK GO（Alpha Researcher）
- 5 VP indicators × 3 lookbacks (24/168/720h) × 8 symbols, 2020-2026, ~47K bars/symbol
- **va_width_pct (VA 寬度) 是唯一有效信號**: IC=+0.024 (24h), 8/8 same sign, A1 6/6 years, A2 shift 9.7%, A3 8/8 consistent
- 傳統 VP S/R 信號全部 FAIL: POC dist IC=-0.004, VA position IC=+0.006, VP skew IC=+0.002 (全 <0.01)
- Quintile spread: va_width_pct +3.84 annualized (8/8 positive), 但非 monotonic
- Filter effect: Above VA Long SR=2.14 >> Inside VA 0.64 → VA position 可作方向 filter
- **G3 混淆因子**: va_width_pct 與 realized volatility corr=0.79-0.85, 本質是 vol regime proxy
  - At 168h: VP IC > Vol IC for all 8/8 symbols (+0.0025 avg) → 有邊際 VP-specific 信息
  - At 24h: Vol IC > VP IC for 4/8 symbols → 短 lookback VP 不如 simple vol
- **Verdict: WEAK GO** — 需 Quant Dev ablation: VP filter vs simple realized vol filter
- Notebook: `notebooks/research/20260228_volume_profile_eda.ipynb`

## 2026-02-27

### 4h TF 維度 CLOSED — 歸檔（Alpha Researcher）
- 修正 HTF look-ahead 後 4h Pure SR 3.97 vs baseline 3.77（Δ=+0.20 僅邊際），PBO 52-67%
- 之前 +1.53 SR 完全來自 HTF filter look-ahead bias。歸檔所有 4h 研究資產

### HTF Filter intra-bar look-ahead 修正（Quant Developer）
- `tsmom_carry_v2_strategy.py` `_apply_htf_filter()` 4h/1d reindex 前加 `shift(1)`
- 修正後 baseline SR 4.31→3.77（-12.5%），live 行為已更新
- `multi_tf_resonance_strategy.py` 同 bug 已修（非生產）

### overlay_params cross-contamination bug 修復（Quant Developer）
- `config.py` `to_backtest_dict()` 改用 `deepcopy`，`run_backtest.py` 額外 deepcopy
- 修正後 Portfolio SR 2.87→4.31，Total Return +187.5%→+343.7%
- 新增 `tests/test_overlay_data_isolation.py`（5 tests）

### 信號顯示修復（DevOps / Quant Developer）
- WebSocket Runner 新增 `_generate_startup_signals()` 解決冷啟動空窗
- 新增 `tests/test_indicator_pipeline.py`（10 tests）
- DevOps 部署流程新增 Post-Deploy Output Verification

### HTF v2 + LSR 部署到 Oracle Cloud（DevOps）
- Config `prod_candidate_htf_lsr.yaml` 替換 `prod_candidate_htf_filter.yaml`
- 重建 `meta_blend_live` + `tg_bot` tmux sessions
- Observation period: 2026-02-27 ~ 2026-03-13

### OI Liq Bounce SHELVED + OI Cascade Overlay（Quant Developer）
- 停止 paper trading（Time-in-market=4.2%，資源效益過低）
- 新增 `oi_cascade_overlay.py`，回測 SR +0.11

### Taker Vol Overlay EDA（Alpha Researcher）
- TVR IC ≈ -0.006~-0.008（弱逆向），CVD IC ≈ -0.013~-0.023（逆向）
- Verdict: WEAK GO — 建議作為 LSR overlay v2 第4確認因子

### LSR Overlay A→D 升級 + 完整驗證 GO_NEXT + Risk APPROVED（全流程）
- OI+FR 確認層升級，WFA 8/8 OOS+, CPCV PBO max 0.13, MC 4/4 PASS
- Config freeze: `prod_candidate_htf_lsr.yaml`

## 2026-02-26

- LSR Confirmatory Overlay 實作 + Meta-Blend 正式回測（Portfolio SR 2.47 +0.21）
- HTF Filter v2 Config Freeze + Validator 修復 + GO_NEXT + Risk APPROVED（SR=2.74）
- LSR Contrarian 策略研究（Portfolio SR=1.39）+ Hybrid Overlay 研究
- Multi-TF + 衍生品數據擴展 Phase 0-4（完整 data/strategy/live pipeline）

## 2026-02-25

- Satellite 策略探索歸檔（BB MR 🔴, Vol Squeeze 🟡, FR Carry 🔴, OI Spike 🔴, OI Liq Bounce ✅）
- 策略組合管理框架（governance doc + compare/generate/portfolio scripts）
- OI Liq Bounce 完整生命週期（研究→驗證→風控→Paper Trading 部署）
- Oracle 多 Runner 數據管線修復（Watchdog 隔離, OI freshness, 記憶體快取）
- 統一 Telegram Bot 模組（MultiStrategyBot, Paper 策略偵測, 排他式歸類）
- Meta-Blend 8-Symbol 生產部署（替換 R3C 10-Symbol）
- 回測數據延伸至 2020 年起 + 交易復盤工具
