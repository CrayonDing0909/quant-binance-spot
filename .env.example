# Binance Spot API keys (for live later; NOT needed for historical klines public endpoints)
BINANCE_API_KEY=your_key
BINANCE_API_SECRET=your_secret

# Data storage
DATA_DIR=./data

# Optional: proxy
HTTP_PROXY=
HTTPS_PROXY=
